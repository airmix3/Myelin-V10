---
phase: 05-langfuse-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/langfuse.ts
  - src/instrumentation.ts
  - src/lib/invoke-agent.ts
  - package.json
autonomous: true
requirements:
  - LANG-01
  - LANG-02
  - LANG-03
  - LANG-04
  - LANG-05
user_setup:
  - service: langfuse-cloud
    why: "Trace export destination for agent observability"
    env_vars:
      - name: LANGFUSE_PUBLIC_KEY
        source: "cloud.langfuse.com -> Settings -> API Keys -> Public Key"
      - name: LANGFUSE_SECRET_KEY
        source: "cloud.langfuse.com -> Settings -> API Keys -> Secret Key"
    dashboard_config:
      - task: "Create a Langfuse Cloud account and project"
        location: "cloud.langfuse.com -> Sign Up -> Create Project"

must_haves:
  truths:
    - "Langfuse OTel SDK initializes once at server boot when API keys are present (D-07, D-08)"
    - "Every invokeAgent() call creates a Langfuse agent observation with taskId, agentId, department metadata (D-10, D-11, D-12)"
    - "Traces are batched and sent async -- agent execution is never blocked by Langfuse (D-09, D-13)"
    - "System works normally when Langfuse API keys are absent -- no errors, no tracing (D-08)"
    - "Multiple invocations for the same task nest under one trace via deterministic traceId from taskId (D-10)"
  artifacts:
    - path: "src/lib/langfuse.ts"
      provides: "Langfuse singleton init, conditional tracing wrapper, isLangfuseEnabled export"
      exports: ["initLangfuse", "isLangfuseEnabled", "withAgentObservation"]
    - path: "src/instrumentation.ts"
      provides: "Langfuse OTel SDK init call before worker loop"
      contains: "initLangfuse"
    - path: "src/lib/invoke-agent.ts"
      provides: "Agent observation wrapping around SDK query() loop"
      contains: "withAgentObservation"
  key_links:
    - from: "src/instrumentation.ts"
      to: "src/lib/langfuse.ts"
      via: "initLangfuse() call"
      pattern: "initLangfuse"
    - from: "src/lib/invoke-agent.ts"
      to: "src/lib/langfuse.ts"
      via: "withAgentObservation() wrapper"
      pattern: "withAgentObservation"
    - from: "src/lib/langfuse.ts"
      to: "@langfuse/otel LangfuseSpanProcessor"
      via: "NodeSDK spanProcessors config"
      pattern: "LangfuseSpanProcessor"
---

<objective>
Install Langfuse SDK v5 packages, create the conditional tracing module, initialize OTel in instrumentation.ts, and instrument invokeAgent() with agent observations.

Purpose: Add hierarchical trace visualization (D-01) to all agent invocations (D-04) via Langfuse Cloud (D-06), providing nested agent/tool/LLM call structure that the flat build logs cannot show.

Output: `src/lib/langfuse.ts` (tracing module), modified `src/instrumentation.ts` (init), modified `src/lib/invoke-agent.ts` (observation wrapping).
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-langfuse-integration/05-CONTEXT.md
@.planning/phases/05-langfuse-integration/05-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From src/lib/events.ts (singleton pattern to replicate):
```typescript
// Singleton -- survives HMR in development
const globalForEvents = globalThis as typeof globalThis & {
  __sseEventBus?: SSEEventBus;
};
export const eventBus: SSEEventBus = globalForEvents.__sseEventBus ??= (() => { ... })();
```

From src/lib/invoke-agent.ts (function to wrap):
```typescript
export interface InvokeAgentOptions {
  taskId: string;
  runId: string;
  agentId: string;
  department: string;
  prompt: string;
  soulMd: string;
  deskDir: string;
  delivDir: string;
  manifestPath: string;
  sessionId?: string;
  maxBudgetUsd?: number;
  outputFormat?: JsonSchemaOutputFormat;
  agents?: Record<string, AgentDefinition>;
}

export interface InvokeAgentResult {
  sessionId: string;
  totalCostUsd: number;
  result?: string;
  structuredOutput?: unknown;
}

export async function invokeAgent(opts: InvokeAgentOptions): Promise<InvokeAgentResult> { ... }
```

From src/instrumentation.ts (bootstrap sequence):
```typescript
export async function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    const g = globalThis as typeof globalThis & { __myelinInit?: boolean };
    if (g.__myelinInit) return;
    g.__myelinInit = true;
    // ... FTS5 init, planning desks, DNA copy, seed agents, orchestrator init, worker loop ...
  }
}
```

From src/lib/logger.ts:
```typescript
export const logger: pino.Logger;
// Usage: logger.child({ module: 'name' })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Langfuse packages and create tracing module</name>
  <files>package.json, src/lib/langfuse.ts</files>
  <read_first>
    - src/lib/events.ts (singleton pattern to replicate for globalThis.__langfuseOtelSdk)
    - src/lib/logger.ts (Pino child logger pattern)
    - src/lib/invoke-agent.ts (InvokeAgentOptions interface -- needed for withAgentObservation signature)
    - package.json (current dependencies)
  </read_first>
  <action>
1. Install Langfuse + OTel packages via pnpm:
   ```
   pnpm add @langfuse/tracing@5.0.1 @langfuse/otel@5.0.1 @opentelemetry/sdk-node@0.214.0 @opentelemetry/api@1.9.1
   ```
   After install, verify single copy of @opentelemetry/api: `pnpm why @opentelemetry/api` (per Pitfall 6 from research).

2. Create `src/lib/langfuse.ts` with these exports:

   **`isLangfuseEnabled(): boolean`** -- returns true if both `LANGFUSE_PUBLIC_KEY` and `LANGFUSE_SECRET_KEY` are set in env (per D-08). Check once at module load, cache in `const LANGFUSE_ENABLED`.

   **`initLangfuse(): Promise<void>`** -- called once from instrumentation.ts (per D-07):
   - If `!LANGFUSE_ENABLED`, log info "Langfuse tracing disabled (no API keys)" via Pino child logger `{ module: 'langfuse' }` and return.
   - Check `(globalThis as any).__langfuseOtelSdk` -- if already set, log debug "already initialized" and return (HMR guard, per Pitfall 2).
   - Dynamic import `@opentelemetry/sdk-node` (NodeSDK) and `@langfuse/otel` (LangfuseSpanProcessor).
   - Create `new NodeSDK({ spanProcessors: [new LangfuseSpanProcessor({ publicKey: process.env.LANGFUSE_PUBLIC_KEY!, secretKey: process.env.LANGFUSE_SECRET_KEY!, baseUrl: process.env.LANGFUSE_BASE_URL ?? 'https://cloud.langfuse.com', exportMode: 'batched', environment: process.env.NODE_ENV ?? 'development' })] })`.
   - Call `sdk.start()`.
   - Store on `(globalThis as any).__langfuseOtelSdk = sdk`.
   - Log info "Langfuse OTel SDK initialized successfully".
   - Wrap entire init in try/catch -- on error, log warn `{ err }` "Langfuse initialization failed -- tracing disabled" (per D-09: fail silently).

   **`withAgentObservation<T>(opts, fn): Promise<T>`** -- wraps an async function in a Langfuse agent observation:
   - Signature: `opts: { taskId: string; agentId: string; department: string; soulMd: string; prompt: string; runId: string }`, `fn: () => Promise<T>`
   - If `!LANGFUSE_ENABLED`, return `fn()` directly (no-op path).
   - Dynamic import `@langfuse/tracing` for `startActiveObservation`, `propagateAttributes`, `createTraceId`.
   - Generate deterministic trace ID: `const traceId = await createTraceId(opts.taskId)` (per D-10, Pitfall 3).
   - Call `startActiveObservation('agent-' + opts.agentId, async (agentObs) => { ... }, { asType: 'agent', parentSpanContext: { traceId, spanId: '0000000000000000', traceFlags: 1 } })`.
   - Inside callback: call `agentObs.update({ input: { prompt: opts.prompt.substring(0, 500) }, metadata: { agentId: opts.agentId, department: opts.department, taskId: opts.taskId, runId: opts.runId, soulExcerpt: opts.soulMd.split('\n').slice(0, 3).join('\n') } })` (per D-11: agent metadata, soul excerpt first 3 lines per specifics).
   - Wrap fn() call in `propagateAttributes({ userId: 'founder', sessionId: opts.taskId, tags: [opts.department, opts.agentId], traceName: 'task-' + opts.taskId }, async () => { ... })` (per Pitfall 5: must be inside startActiveObservation callback).
   - On success: `agentObs.update({ output: { success: true } })`.
   - On error: `agentObs.update({ level: 'ERROR', statusMessage: String(err) })`, then rethrow.
   - Wrap entire function in try/catch at the outer level -- if Langfuse itself errors (import fails, API issue), log warn and fall through to `fn()` directly (per D-09).
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json','utf8')); const deps = pkg.dependencies || {}; console.log('@langfuse/tracing:', deps['@langfuse/tracing']); console.log('@langfuse/otel:', deps['@langfuse/otel']); console.log('@opentelemetry/sdk-node:', deps['@opentelemetry/sdk-node']); console.log('@opentelemetry/api:', deps['@opentelemetry/api']); const lf = fs.readFileSync('src/lib/langfuse.ts','utf8'); console.log('has initLangfuse:', lf.includes('export async function initLangfuse')); console.log('has isLangfuseEnabled:', lf.includes('export function isLangfuseEnabled')); console.log('has withAgentObservation:', lf.includes('export async function withAgentObservation')); console.log('has globalThis guard:', lf.includes('__langfuseOtelSdk')); console.log('has LangfuseSpanProcessor:', lf.includes('LangfuseSpanProcessor')); console.log('has createTraceId:', lf.includes('createTraceId')); console.log('has propagateAttributes:', lf.includes('propagateAttributes'));"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@langfuse/tracing": "5.0.1"`, `"@langfuse/otel": "5.0.1"`, `"@opentelemetry/sdk-node": "0.214.0"`, `"@opentelemetry/api": "1.9.1"`
    - src/lib/langfuse.ts exports `initLangfuse`, `isLangfuseEnabled`, `withAgentObservation`
    - src/lib/langfuse.ts contains `__langfuseOtelSdk` singleton guard on globalThis
    - src/lib/langfuse.ts contains `LangfuseSpanProcessor` with `exportMode: 'batched'`
    - src/lib/langfuse.ts contains `createTraceId(opts.taskId)` for deterministic trace-task correlation
    - src/lib/langfuse.ts contains `propagateAttributes` call inside `startActiveObservation` callback
    - src/lib/langfuse.ts contains `LANGFUSE_PUBLIC_KEY` and `LANGFUSE_SECRET_KEY` env var checks
    - src/lib/langfuse.ts contains try/catch around all Langfuse operations with Pino warn logging on failure
    - `pnpm why @opentelemetry/api` shows single version (no duplicates)
  </acceptance_criteria>
  <done>Langfuse packages installed, langfuse.ts module exists with all three exported functions, conditional activation via env vars, HMR-safe singleton, and fail-silent error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Langfuse init into instrumentation.ts and wrap invokeAgent()</name>
  <files>src/instrumentation.ts, src/lib/invoke-agent.ts</files>
  <read_first>
    - src/instrumentation.ts (current bootstrap sequence -- Langfuse init goes after step 5 orchestrator, before step 6 worker loop)
    - src/lib/invoke-agent.ts (current invokeAgent function -- wrap the query loop in withAgentObservation)
    - src/lib/langfuse.ts (just created in Task 1 -- verify exports match what we import)
  </read_first>
  <action>
1. **Modify `src/instrumentation.ts`** -- add Langfuse init as step 5.5 (after orchestrator init at step 5, before worker loop at step 6, per Pitfall 1: OTel must init before any traces):
   - Add dynamic import: `const { initLangfuse } = await import('./lib/langfuse');`
   - Call `await initLangfuse();` in a try/catch block matching the existing pattern.
   - Log: `log.info('Langfuse tracing initialized');` on success, `log.error({ err }, 'Langfuse initialization failed');` on failure.
   - Add comment: `// 5.5. Initialize Langfuse OTel tracing (D-07, must be before worker loop per Pitfall 1)`

2. **Modify `src/lib/invoke-agent.ts`** -- wrap the core query loop in `withAgentObservation`:
   - Add import at top: `import { withAgentObservation } from '@/lib/langfuse';`
   - In `invokeAgent()` function: wrap lines 100-221 (from `const q = query(...)` through the `return` in the result handler) inside `withAgentObservation()`.
   - The wrapper call: `return withAgentObservation({ taskId: opts.taskId, agentId: opts.agentId, department: opts.department, soulMd: opts.soulMd, prompt: opts.prompt, runId: opts.runId }, async () => { /* existing lines 100-224 */ });`
   - The function body before line 100 (ToolContext creation, MCP server, access control, log.info, queryOptions) stays OUTSIDE the observation wrapper -- it's setup, not the traced execution.
   - The observation captures: SDK query execution, all message iteration, cost extraction, session storage, and the return value.
   - Do NOT add Langfuse generation/tool observations inside the message loop (per Pitfall 4: SDK subprocess LLM calls are not visible to OTel context -- we capture aggregate data from result message only). The `withAgentObservation` wrapper captures the outer invocation as one agent span with cost/token metadata from the result.

3. After modification, add a **generation observation** inside `withAgentObservation` for the result message cost data. In `src/lib/langfuse.ts`, update `withAgentObservation` to accept an optional `onResult` callback that the caller can use to record generation data. Actually, simpler approach: after `fn()` completes, the result is returned through `withAgentObservation`. The wrapper does NOT need per-message hooks. The outer agent observation already captures start/end time and success/error. Cost data is already in cost_events table (D-02 secondary value comes from Langfuse Cloud analytics on the traces, not from duplicating cost_events).
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && node -e "const fs = require('fs'); const inst = fs.readFileSync('src/instrumentation.ts','utf8'); console.log('has initLangfuse import:', inst.includes('initLangfuse')); console.log('has initLangfuse call:', inst.includes('await initLangfuse()')); console.log('init before worker:', inst.indexOf('initLangfuse') < inst.indexOf('startWorkerLoop')); const ia = fs.readFileSync('src/lib/invoke-agent.ts','utf8'); console.log('has withAgentObservation import:', ia.includes('withAgentObservation')); console.log('has withAgentObservation call:', ia.includes('return withAgentObservation(') || ia.includes('return await withAgentObservation('));"</automated>
  </verify>
  <acceptance_criteria>
    - src/instrumentation.ts contains `await initLangfuse()` call between orchestrator init and worker loop start
    - src/instrumentation.ts contains `import('./lib/langfuse')` dynamic import
    - src/instrumentation.ts Langfuse init is wrapped in try/catch (matches existing pattern)
    - src/lib/invoke-agent.ts contains `import { withAgentObservation } from '@/lib/langfuse'`
    - src/lib/invoke-agent.ts wraps the query() execution loop in `withAgentObservation({ taskId: opts.taskId, agentId: opts.agentId, ... })`
    - src/lib/invoke-agent.ts: setup code (ToolContext, MCP server, access control, queryOptions) remains OUTSIDE the observation wrapper
    - No generation/tool child observations inside the message loop (per Pitfall 4)
  </acceptance_criteria>
  <done>instrumentation.ts calls initLangfuse() before worker loop; invokeAgent() wraps its query execution in withAgentObservation() so every agent invocation creates a Langfuse trace span.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without TypeScript errors (Langfuse types resolve correctly)
2. Without LANGFUSE_PUBLIC_KEY set: server starts normally, no Langfuse errors in logs, invokeAgent works unchanged
3. With LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY set: server logs "Langfuse OTel SDK initialized successfully" on startup
4. `grep -r 'initLangfuse\|withAgentObservation\|isLangfuseEnabled' src/` shows all three functions used
</verification>

<success_criteria>
- Langfuse SDK packages installed and importable
- Conditional tracing: enabled with keys, silently disabled without
- Every invokeAgent() call produces a Langfuse agent observation when tracing is enabled
- Traces use taskId-derived deterministic trace IDs for correlation
- Server boots and agents execute normally regardless of Langfuse availability
</success_criteria>

<output>
After completion, create `.planning/phases/05-langfuse-integration/05-01-SUMMARY.md`
</output>
