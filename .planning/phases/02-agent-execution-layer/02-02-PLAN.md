---
phase: 02-agent-execution-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/tamir/soul.md
  - src/agents/tamir/card.json
  - src/agents/tamir/agent.ts
  - src/agents/cto/soul.md
  - src/agents/cto/card.json
  - src/agents/cto/agent.ts
  - src/agents/cmo/soul.md
  - src/agents/cmo/card.json
  - src/agents/cmo/agent.ts
  - src/agents/coo/soul.md
  - src/agents/coo/card.json
  - src/agents/coo/agent.ts
  - src/lib/seed-agents.ts
autonomous: true
requirements: [AGENT-03]

must_haves:
  truths:
    - "4 agent directories exist with soul.md, card.json, and agent.ts each"
    - "soul.md files are 2-4 pages comprehensive guides per D-03"
    - "Tamir is direct and substantive per D-04"
    - "CTO, CMO, COO are domain experts with strong opinions per D-05"
    - "Agent names are role titles only per D-06"
    - "Seed script inserts 4 executive employees into the database"
  artifacts:
    - path: "src/agents/tamir/soul.md"
      provides: "Tamir Chief of Staff persona and behavior guide"
      min_lines: 80
    - path: "src/agents/tamir/card.json"
      provides: "Tamir AgentCard metadata"
      contains: "tamir"
    - path: "src/agents/tamir/agent.ts"
      provides: "Tamir agent configuration export"
      exports: ["agentConfig"]
    - path: "src/agents/cto/soul.md"
      provides: "CTO persona and behavior guide"
      min_lines: 80
    - path: "src/agents/cmo/soul.md"
      provides: "CMO persona and behavior guide"
      min_lines: 80
    - path: "src/agents/coo/soul.md"
      provides: "COO persona and behavior guide"
      min_lines: 80
    - path: "src/lib/seed-agents.ts"
      provides: "Database seed function for 4 executives"
      exports: ["seedAgents"]
  key_links:
    - from: "src/agents/*/agent.ts"
      to: "src/agents/*/soul.md"
      via: "reads soul.md content for systemPrompt"
      pattern: "readFileSync.*soul\\.md"
    - from: "src/lib/seed-agents.ts"
      to: "src/agents/*/card.json"
      via: "reads card.json for employee record creation"
      pattern: "card\\.json"
---

<objective>
Agent identity: 4 executive agent directories with comprehensive soul.md, card.json, agent.ts, plus database seed script.

Purpose: Gives each agent a rich identity that drives behavior through systemPrompt injection. Per D-03, soul.md files are comprehensive 2-4 page guides, not minimal personas.

Output: 12 agent files (4 agents x 3 files) and 1 seed script.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-execution-layer/02-CONTEXT.md
@.planning/phases/02-agent-execution-layer/02-RESEARCH.md

<interfaces>
From src/lib/db.ts:
```typescript
export const prisma: PrismaClient;
```

From src/lib/id.ts:
```typescript
export function generateId(prefix: string): string;
```

From prisma/schema.prisma (Employee model):
```prisma
model Employee {
  id          String   @id
  name        String
  role        String                // executive | temp
  department  String                // tech | marketing | operations | global
  agentId     String?  @unique      // e.g., "cto", "cmo", "coo", "tamir"
  status      String   @default("active")
  budgetLimit Float    @default(10.0)
  budgetSpent Float    @default(0.0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

From src/a2a/types.ts (created by Plan 01, but this plan is Wave 1 parallel -- agent.ts files should define their own AgentConfig type locally or import from card.json directly):
```typescript
export interface AgentCard {
  agentId: string;
  name: string;
  department: string;
  role: 'executive' | 'temp';
  description: string;
  tools: string[];
  skills: string[];
  avatarColor: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four executive agent directories with soul.md, card.json, and agent.ts plus seed script</name>
  <files>
    src/agents/tamir/soul.md, src/agents/tamir/card.json, src/agents/tamir/agent.ts,
    src/agents/cto/soul.md, src/agents/cto/card.json, src/agents/cto/agent.ts,
    src/agents/cmo/soul.md, src/agents/cmo/card.json, src/agents/cmo/agent.ts,
    src/agents/coo/soul.md, src/agents/coo/card.json, src/agents/coo/agent.ts,
    src/lib/seed-agents.ts
  </files>
  <read_first>
    .planning/phases/02-agent-execution-layer/02-CONTEXT.md (D-03 through D-06: soul.md design decisions, persona directions)
    .planning/REQUIREMENTS.md (AGENT-03: agent directory specs)
    .planning/PROJECT.md (company context for soul.md content -- department descriptions, company stage, working style)
    config/company-dna.template.md (company DNA that agents should be aware of)
    prisma/schema.prisma (Employee model for seed script)
    src/lib/db.ts (prisma import for seed)
    src/lib/id.ts (generateId for employee IDs)
  </read_first>
  <action>
Create `src/agents/` directory with 4 subdirectories. For each agent:

**card.json** -- AgentCard metadata:

Tamir:
```json
{
  "agentId": "tamir",
  "name": "Tamir",
  "department": "global",
  "role": "executive",
  "description": "Chief of Staff. Routes tasks, manages planning flow, monitors system health.",
  "tools": ["read_memory", "write_memory", "read_knowledge", "write_knowledge", "search_knowledge", "file_to_vault", "read_inbox", "get_dept_status"],
  "skills": ["memory-management"],
  "avatarColor": "#FF4444"
}
```

CTO (per D-06, name is role title only):
```json
{
  "agentId": "cto",
  "name": "CTO",
  "department": "tech",
  "role": "executive",
  "description": "Chief Technology Officer. Owns technical architecture, code quality, and engineering execution.",
  "tools": ["read_memory", "write_memory", "read_knowledge", "write_knowledge", "search_knowledge", "promote_to_deliverable", "submit_for_review", "approve_deliverable", "request_changes", "file_to_vault", "propose_skill", "hire_employee"],
  "skills": ["memory-management", "skill-extractor"],
  "avatarColor": "#4488FF"
}
```

CMO:
```json
{
  "agentId": "cmo",
  "name": "CMO",
  "department": "marketing",
  "role": "executive",
  "description": "Chief Marketing Officer. Owns brand narrative, content strategy, and audience engagement.",
  "tools": ["read_memory", "write_memory", "read_knowledge", "write_knowledge", "search_knowledge", "promote_to_deliverable", "submit_for_review", "approve_deliverable", "request_changes", "file_to_vault", "propose_skill", "hire_employee"],
  "skills": ["memory-management", "skill-extractor"],
  "avatarColor": "#FF44AA"
}
```

COO:
```json
{
  "agentId": "coo",
  "name": "COO",
  "department": "operations",
  "role": "executive",
  "description": "Chief Operating Officer. Owns systems, processes, analytics, and operational efficiency.",
  "tools": ["read_memory", "write_memory", "read_knowledge", "write_knowledge", "search_knowledge", "promote_to_deliverable", "submit_for_review", "approve_deliverable", "request_changes", "file_to_vault", "propose_skill", "hire_employee"],
  "skills": ["memory-management", "skill-extractor"],
  "avatarColor": "#44CC88"
}
```

**soul.md** -- Per D-03, comprehensive 2-4 page guides. Each soul.md must include:
1. **Identity** -- who you are, your role in Myelin, your relationship to the CEO (Omer)
2. **Personality & Tone** -- per D-04 (Tamir: direct+substantive) and D-05 (others: domain experts with strong opinions)
3. **Decision-Making Philosophy** -- how you approach problems, what tradeoffs you favor
4. **Department Expertise** -- deep knowledge of your domain
5. **Tool Usage Patterns** -- when and how to use your available tools
6. **Cross-Department Behavior** -- how to interact with other departments
7. **Handling Ambiguity** -- what to do when instructions are unclear
8. **Working With Temp Employees** -- how to delegate, supervise, review (for dept heads)

Tamir soul.md specifics per D-04 and CONTEXT.md specifics section:
- Sharp operator, direct, no filler but substantive
- Not "Got it, routing to CTO" but "Routing to CTO -- this maps to the EEG data pipeline work. CTO will need the dataset spec before starting; I've flagged that in the task context."
- Manages routing, planning flow, system health
- Does NOT execute tasks -- routes and monitors
- system-reset is an admin operation, not a task (per CONTEXT.md specifics)

CTO soul.md per D-05:
- Thinks in architecture tradeoffs
- Pushes back when something doesn't make sense technically
- Strong opinions on code quality, testing, technical debt
- Prefers async patterns, clean interfaces, composition over inheritance

CMO soul.md per D-05:
- Thinks in narratives and audience
- Pushes back when messaging doesn't serve the target audience
- Strong opinions on brand voice, content quality, channel strategy
- Understands that Myelin is pre-product neurotech -- content must educate + excite

COO soul.md per D-05:
- Thinks in systems and second-order effects
- Pushes back when processes create overhead without ROI
- Strong opinions on measurement, efficiency, automation
- Focuses on what compounds -- processes, data pipelines, operational metrics

**agent.ts** -- Configuration export for each agent:

```typescript
import { readFileSync } from 'fs';
import { resolve, dirname } from 'path';
import { fileURLToPath } from 'url';

// Agent card metadata
import cardJson from './card.json';

const __dirname = dirname(fileURLToPath(import.meta.url));

export interface AgentConfig {
  agentId: string;
  name: string;
  department: string;
  role: 'executive' | 'temp';
  description: string;
  tools: string[];
  skills: string[];
  avatarColor: string;
  soulMd: string;
}

export function getAgentConfig(): AgentConfig {
  const soulMd = readFileSync(resolve(__dirname, 'soul.md'), 'utf-8');
  return {
    ...cardJson,
    soulMd,
  };
}

export const agentConfig = getAgentConfig();
```

Each agent.ts follows this exact pattern. The `import cardJson from './card.json'` requires `"resolveJsonModule": true` in tsconfig.json -- verify this is set (it should be from Next.js defaults).

**src/lib/seed-agents.ts** -- Seeds 4 executives to DB:

```typescript
import { prisma } from '@/lib/db';
import { generateId } from '@/lib/id';
import { logger } from '@/lib/logger';

const EXECUTIVES = [
  { agentId: 'tamir', name: 'Tamir', department: 'global', budgetLimit: 50.0 },
  { agentId: 'cto', name: 'CTO', department: 'tech', budgetLimit: 25.0 },
  { agentId: 'cmo', name: 'CMO', department: 'marketing', budgetLimit: 25.0 },
  { agentId: 'coo', name: 'COO', department: 'operations', budgetLimit: 25.0 },
];

export async function seedAgents(): Promise<void> {
  const log = logger.child({ module: 'seed-agents' });
  for (const exec of EXECUTIVES) {
    const existing = await prisma.employee.findFirst({ where: { agentId: exec.agentId } });
    if (!existing) {
      await prisma.employee.create({
        data: {
          id: generateId('emp'),
          name: exec.name,
          role: 'executive',
          department: exec.department,
          agentId: exec.agentId,
          status: 'active',
          budgetLimit: exec.budgetLimit,
          budgetSpent: 0.0,
        },
      });
      log.info({ agentId: exec.agentId }, 'Seeded executive agent');
    } else {
      log.debug({ agentId: exec.agentId }, 'Executive agent already exists');
    }
  }
}
```

This function should be called from instrumentation.ts (Phase 2 wiring in Plan 03).
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && ls src/agents/tamir/soul.md src/agents/cto/soul.md src/agents/cmo/soul.md src/agents/coo/soul.md src/agents/tamir/card.json src/agents/cto/card.json src/agents/cmo/card.json src/agents/coo/card.json src/agents/tamir/agent.ts src/agents/cto/agent.ts src/agents/cmo/agent.ts src/agents/coo/agent.ts src/lib/seed-agents.ts 2>&1 && wc -l src/agents/*/soul.md</automated>
  </verify>
  <acceptance_criteria>
    - src/agents/tamir/soul.md exists and has at least 80 lines
    - src/agents/cto/soul.md exists and has at least 80 lines
    - src/agents/cmo/soul.md exists and has at least 80 lines
    - src/agents/coo/soul.md exists and has at least 80 lines
    - src/agents/tamir/card.json contains `"agentId": "tamir"` and `"avatarColor": "#FF4444"`
    - src/agents/cto/card.json contains `"agentId": "cto"` and `"department": "tech"`
    - src/agents/cmo/card.json contains `"agentId": "cmo"` and `"department": "marketing"`
    - src/agents/coo/card.json contains `"agentId": "coo"` and `"department": "operations"`
    - All 4 agent.ts files export `agentConfig` and `getAgentConfig`
    - src/lib/seed-agents.ts exports `seedAgents` function
    - src/lib/seed-agents.ts contains `prisma.employee.create` and `prisma.employee.findFirst`
    - Tamir soul.md does NOT contain "Got it" or similar filler phrases
    - CTO soul.md contains "architecture" or "tradeoff"
    - CMO soul.md contains "narrative" or "audience"
    - COO soul.md contains "systems" or "second-order"
  </acceptance_criteria>
  <done>4 executive agent directories with comprehensive soul.md (80+ lines each), card.json, and agent.ts. Seed script ready to insert executives. Per D-03, D-04, D-05, D-06.</done>
</task>

</tasks>

<verification>
1. All 13 files exist at their specified paths
2. Each soul.md is 80+ lines (comprehensive per D-03)
3. Each card.json is valid JSON with agentId field
4. Each agent.ts exports agentConfig
5. seed-agents.ts compiles (npx tsc --noEmit)
</verification>

<success_criteria>
- 4 comprehensive agent identities with distinct personalities
- Tamir voice: direct + substantive, not filler (per D-04)
- Dept heads: domain experts with strong opinions (per D-05)
- Names are role titles only (per D-06)
- Seed script ready for instrumentation.ts integration
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-execution-layer/02-02-SUMMARY.md`
</output>
