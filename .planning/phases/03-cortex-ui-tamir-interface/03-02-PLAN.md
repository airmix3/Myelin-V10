---
phase: 03-cortex-ui-tamir-interface
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/page.tsx
  - src/app/deliverables/page.tsx
  - src/app/vault/page.tsx
  - src/app/api/vault/search/route.ts
autonomous: true
requirements: [UI-03, UI-04, UI-06]

must_haves:
  truths:
    - "Dashboard shows live agent status with colored dots and activity timeline"
    - "Deliverables gallery shows card grid with dept badges and status indicators"
    - "Vault page has FTS5 search with 300ms debounce returning highlighted results"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Dashboard page with stats, agent status, and activity timeline"
      contains: "getDashboardStats"
    - path: "src/app/deliverables/page.tsx"
      provides: "Deliverables gallery with card grid and filters"
      contains: "deliverable"
    - path: "src/app/vault/page.tsx"
      provides: "Vault search page with FTS5"
      contains: "use client"
    - path: "src/app/api/vault/search/route.ts"
      provides: "Vault search API using FTS5"
      contains: "searchDocuments"
  key_links:
    - from: "src/app/page.tsx"
      to: "prisma"
      via: "Server Component data fetch"
      pattern: "prisma\\.(employee|task|deliverable)"
    - from: "src/app/page.tsx"
      to: "src/components/useSSE.ts"
      via: "Client island SSE subscription"
      pattern: "useSSE"
    - from: "src/app/vault/page.tsx"
      to: "src/app/api/vault/search/route.ts"
      via: "fetch with debounced query"
      pattern: "api/vault/search"
    - from: "src/app/api/vault/search/route.ts"
      to: "src/lib/fts.ts"
      via: "searchDocuments function"
      pattern: "searchDocuments"
---

<objective>
Build the Dashboard, Deliverables Gallery, and Vault pages — three data-display pages that share similar patterns (server data fetch + client interactivity).

Purpose: These are the primary read-only views CEO uses to monitor the system. Dashboard is the landing page (D-13), Deliverables shows task output, Vault provides full-text search across all indexed documents.
Output: Three functional pages at `/`, `/deliverables`, `/vault` plus the vault search API endpoint.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-cortex-ui-tamir-interface/03-CONTEXT.md
@.planning/phases/03-cortex-ui-tamir-interface/03-UI-SPEC.md
@.planning/phases/03-cortex-ui-tamir-interface/03-01-SUMMARY.md

<interfaces>
<!-- From src/lib/db.ts -->
<!-- export const prisma: PrismaClient -->
<!-- export const sqlite: Database -->

<!-- From src/lib/fts.ts -->
<!-- export function searchDocuments(query: string, limit?: number): SearchResult[] -->
<!-- SearchResult = { id, title, source, department, snippet, rank } -->

<!-- From src/components/useSSE.ts (created in Plan 01) -->
<!-- export function useSSE(handlers: Record<string, SSEHandler>) -->

<!-- Prisma models (from schema.prisma): -->
<!-- Employee: id, name, role, department, agentId, status, budgetLimit, budgetSpent -->
<!-- Task: id, title, description, department, state, metadata, planningAgentId, executorAgentId, currentActorId, createdAt -->
<!-- Deliverable: id, taskId, title, type, status, department, creatorId, primaryFile, createdAt -->
<!-- HireRequest: id, taskId, status (pending|approved|rejected) -->
<!-- ActivityLog: id, taskId, agentId, actionType, description, metadata, createdAt -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard page with stats, agent status, and activity timeline</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current placeholder — will be overwritten)
    - src/lib/db.ts (prisma import path)
    - src/lib/fts.ts (for SearchResult type reference)
    - src/components/useSSE.ts (SSE hook from Plan 01)
    - prisma/schema.prisma (table shapes: Employee, Task, Deliverable, HireRequest, ActivityLog)
    - .planning/phases/03-cortex-ui-tamir-interface/03-UI-SPEC.md (Dashboard interaction contract and empty states)
    - .planning/phases/03-cortex-ui-tamir-interface/03-CONTEXT.md (D-13, D-15)
  </read_first>
  <action>
Create `src/app/page.tsx` using the Server Component + Client Island pattern (per D-13: Dashboard is the default landing page `/`).

**Server Component** (default export `Dashboard`):
- Fetch dashboard data using `Promise.all()`:
  ```typescript
  const [activeAgents, activeTasks, pendingApprovals, deliverableCount, agents, activities] = await Promise.all([
    prisma.employee.count({ where: { status: 'active' } }),
    prisma.task.count({ where: { state: { in: ['submitted', 'working', 'input-required'] } } }),
    prisma.hireRequest.count({ where: { status: 'pending' } }),
    prisma.deliverable.count(),
    prisma.employee.findMany({ where: { status: 'active' }, select: { id: true, name: true, agentId: true, department: true } }),
    prisma.activityLog.findMany({ orderBy: { createdAt: 'desc' }, take: 10, select: { id: true, taskId: true, agentId: true, actionType: true, description: true, createdAt: true } }),
  ]);
  ```
- Pass all data as props to `<DashboardClient>` (Client Component defined in same file or separate)

**Client Component** (`DashboardClient` — 'use client'):
- **Stats row**: 4 stat cards in `.stat-row`:
  - Active Agents (`stat-value` + `stat-label`)
  - Active Tasks
  - Pending Approvals
  - Deliverables
  - Use `--` as placeholder while loading (per UI-SPEC loading states)

- **Agent Status Panel** (per D-15):
  - `.card` with `.card-title` "AGENT STATUS"
  - List each active employee with:
    - `.dot` colored by last activity age: green if activity < 30s, amber if > 30s, gray if no recent activity
    - Agent name and department badge
  - Track heartbeat via SSE: `useSSE({ 'task:heartbeat': (data) => updateAgentTimestamp(data.agentId, Date.now()), 'agent:invoked': (data) => updateAgentTimestamp(data.agentId, Date.now()) })`
  - Store last-seen timestamps in `useState<Record<string, number>>({})`
  - Update dot colors every 10 seconds via `setInterval`
  - Empty state: "No active agents" / "Agents will appear here when tasks are running."

- **Recent Activity Timeline** (per D-15):
  - `.card` with `.card-title` "RECENT ACTIVITY"
  - List last 10 activity_log entries with: action type badge, description, relative timestamp
  - Update via SSE: `'task:transition': (data) => prependActivity(data)`
  - Format timestamps as relative ("2m ago", "1h ago") using simple date math
  - Empty state: "No recent activity" / "Activity will appear here once agents start executing tasks."
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20 && echo "TypeCheck done"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx exists as a Server Component (no 'use client' at top level)
    - Contains `prisma.employee.count` call for active agents stat
    - Contains `prisma.task.count` with `state: { in: ['submitted', 'working', 'input-required'] }` filter
    - Contains `prisma.hireRequest.count` with `status: 'pending'` filter
    - Contains `prisma.deliverable.count` call
    - Contains `prisma.activityLog.findMany` with `take: 10` and `orderBy: { createdAt: 'desc' }`
    - Client island contains `useSSE` import and call with `'task:transition'` and/or `'task:heartbeat'` handlers
    - Contains `.stat-row` with 4 stat cards
    - Contains `.dot-green`, `.dot-amber`, `.dot-gray` class references for agent status dots
    - Contains empty state text: "No active agents" and "No recent activity"
  </acceptance_criteria>
  <done>Dashboard page loads stats from DB, shows agent status with colored dots, displays activity timeline, and updates live via SSE</done>
</task>

<task type="auto">
  <name>Task 2: Build Deliverables Gallery page</name>
  <files>src/app/deliverables/page.tsx</files>
  <read_first>
    - prisma/schema.prisma (Deliverable model: id, taskId, title, type, status, department, creatorId, primaryFile, createdAt)
    - src/lib/db.ts (prisma import)
    - .planning/phases/03-cortex-ui-tamir-interface/03-UI-SPEC.md (Deliverables Gallery interaction contract)
    - .planning/phases/03-cortex-ui-tamir-interface/03-CONTEXT.md (UI-04 requirements)
  </read_first>
  <action>
Create `src/app/deliverables/page.tsx` as a Server Component with a Client island for search and filtering.

**Server Component** (default export):
- Fetch all deliverables: `prisma.deliverable.findMany({ orderBy: { createdAt: 'desc' }, include: { task: { select: { title: true, state: true } } } })`
- Pass deliverables to `<DeliverablesClient>`

**Client Component** (`DeliverablesClient` — 'use client', can be in same file):
- **Page heading**: `<h1>Deliverables</h1>`
- **Filter tabs** (`.tab-bar`): "All", "Tech", "Marketing", "Operations" tabs
  - Track active tab in `useState<string>('all')`
  - Filter deliverables by department when tab selected
- **Search input**: `.search-bar` with 300ms debounce using `useState` + `useEffect` with `setTimeout`/`clearTimeout`
  - Filter deliverables where `title` includes search term (case-insensitive)
- **Card grid**: `<div className="grid-3">` with deliverable cards
  - Each card: `.card` with:
    - Title (bold, 13px)
    - Department badge (`.badge-tech`, `.badge-marketing`, `.badge-ops`)
    - Type badge if type is set (e.g., "VIDEO", "DOCUMENT", "CODE")
    - Status: `.badge-active` for in-progress, `.badge-done` for completed, `.badge-pending` for reviewed
    - Creator name (from creatorId — display as-is since it's an agent ID like "cto")
    - Date: formatted `createdAt`
    - In-progress tasks: add `style={{ borderLeft: '3px solid var(--amber)' }}` per UI-04
  - Sort: in-progress deliverables at top, then by createdAt descending
  - Each card links to `/deliverables/${deliverable.id}` (Phase 4 page — just make it a link)
- **Empty state**: "No deliverables yet" / "Submit a task through Tamir to see deliverables here."
  </action>
  <verify>
    <automated>test -f src/app/deliverables/page.tsx && grep "deliverable" src/app/deliverables/page.tsx | head -5 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/deliverables/page.tsx exists
    - Contains `prisma.deliverable.findMany` call
    - Contains `.tab-bar` with department filter tabs including "All", "Tech", "Marketing", "Operations"
    - Contains `.search-bar` or `input` with debounce logic (setTimeout/clearTimeout pattern)
    - Contains `.grid-3` class for card grid layout
    - Contains `.badge-tech`, `.badge-marketing`, `.badge-ops` department badge references
    - Contains amber left border for in-progress items (`borderLeft` with `var(--amber)`)
    - Contains link to `/deliverables/` for card navigation
    - Contains empty state text: "No deliverables yet"
  </acceptance_criteria>
  <done>Deliverables gallery shows card grid with department filters, search, and in-progress items highlighted</done>
</task>

<task type="auto">
  <name>Task 3: Build Vault page with FTS5 search and search API</name>
  <files>src/app/vault/page.tsx, src/app/api/vault/search/route.ts</files>
  <read_first>
    - src/lib/fts.ts (searchDocuments function and SearchResult interface)
    - src/lib/db.ts (prisma for document listing)
    - prisma/schema.prisma (Document model: id, title, content, source, department, filedBy, filePath, createdAt)
    - .planning/phases/03-cortex-ui-tamir-interface/03-UI-SPEC.md (Vault interaction contract and empty states)
  </read_first>
  <action>
**API Route** (`src/app/api/vault/search/route.ts`):
```typescript
import { searchDocuments } from '@/lib/fts';
import { NextRequest, NextResponse } from 'next/server';

export async function GET(request: NextRequest) {
  const query = request.nextUrl.searchParams.get('q') || '';
  if (!query.trim()) {
    return NextResponse.json({ results: [] });
  }
  try {
    const results = searchDocuments(query, 20);
    return NextResponse.json({ results });
  } catch (error) {
    return NextResponse.json({ results: [], error: 'Search is temporarily unavailable.' }, { status: 500 });
  }
}
```

**Vault Page** (`src/app/vault/page.tsx`): Full Client Component ('use client') since search is the primary interaction.

- **Page heading**: `<h1>Vault</h1>`
- **Search input**: `<input type="text" className="search-bar" placeholder="Search documents..." style={{ width: '100%', marginBottom: '24px' }} />`
  - 300ms debounce: `useState` for input value, `useEffect` with `setTimeout` that calls `fetch('/api/vault/search?q=${encodeURIComponent(debouncedQuery)}')`
  - Show "Searching..." text in `--text-dim` while fetching

- **Document list** (when search has results):
  - List each result as a `.card` that is expandable (click to toggle full content):
    - Title (bold)
    - Row: department badge, source badge (vault/knowledge/deliverable), filed_by, formatted date
    - Snippet with `<mark>` highlights (from FTS5 snippet function) rendered via `dangerouslySetInnerHTML`
    - On click: expand to show full document content rendered via `marked.parse()` with `.md-render` class
    - Track expanded document ID in `useState<string | null>(null)`
  - For full content on expand: make a secondary fetch to get the document content, OR load all documents initially. Simpler: fetch document by ID from prisma when expanded. Create inline API or use existing document fetch.
  - Alternative simpler approach: Pre-load all documents on mount for the initial list, use client-side FTS API for search.

- **Initial state** (no search query): List all documents from the database:
  - Fetch on mount: `fetch('/api/vault/search?q=')` returns empty, so instead fetch all documents via a separate effect or server-side initial load
  - Better pattern: make the page a Server Component that pre-loads documents, passes to Client island
  - Simplest: Vault page fetches initial documents on mount via `fetch('/api/vault/documents')` or inline

  Actually simplest approach: make it a Server Component that pre-loads documents, passes to VaultClient:
  - Server: `const documents = await prisma.document.findMany({ orderBy: { createdAt: 'desc' }, select: { id: true, title: true, source: true, department: true, filedBy: true, content: true, createdAt: true } })`
  - Client: shows all documents initially, filters via FTS5 API when user types search query

- **Empty states**:
  - No documents at all: "Vault is empty" / "Documents filed by agents will appear here. Company DNA is loaded on first boot."
  - Search with no results: "No documents found" / "Try a different search term, or file documents through agent tasks."

- **Markdown rendering** on expand: `import { marked } from 'marked'` and render with `<div className="md-render" dangerouslySetInnerHTML={{ __html: marked.parse(doc.content) }} />`
  </action>
  <verify>
    <automated>test -f src/app/vault/page.tsx && test -f src/app/api/vault/search/route.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/vault/page.tsx exists
    - src/app/api/vault/search/route.ts exists with GET handler
    - API route imports `searchDocuments` from `@/lib/fts`
    - API route accepts `q` query parameter
    - Vault page contains search input with `search-bar` class or similar
    - Contains debounce logic (setTimeout/clearTimeout with ~300ms delay)
    - Contains `marked.parse` or `marked` import for markdown rendering
    - Contains `.md-render` class for rendered content
    - Contains `dangerouslySetInnerHTML` for FTS5 snippet highlights
    - Contains empty state text: "Vault is empty" and "No documents found"
    - Contains expandable card pattern (click to show full content)
  </acceptance_criteria>
  <done>Vault page has working FTS5 search with debounce, document list with expandable markdown preview, and proper empty states</done>
</task>

</tasks>

<verification>
1. `pnpm dev` — navigate to `/`: Dashboard shows 4 stat cards, agent status panel, activity timeline
2. Navigate to `/deliverables`: Gallery shows card grid (or empty state if no deliverables)
3. Navigate to `/vault`: Search input works, documents display (or empty state), clicking expands markdown
4. TypeScript compiles without errors
</verification>

<success_criteria>
- Dashboard loads real data from Prisma and updates via SSE
- Deliverables gallery has working department filter tabs and search
- Vault page searches documents via FTS5 with 300ms debounce
- All pages use cortex.css classes (no inline style frameworks)
- Empty states show correct copy from UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/03-cortex-ui-tamir-interface/03-02-SUMMARY.md`
</output>
