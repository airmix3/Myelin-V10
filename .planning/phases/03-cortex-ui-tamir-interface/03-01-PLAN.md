---
phase: 03-cortex-ui-tamir-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/cortex.css
  - src/app/layout.tsx
  - src/components/Sidebar.tsx
  - src/components/useSSE.ts
autonomous: true
requirements: [UI-01, UI-02, UI-10]

must_haves:
  truths:
    - "All pages share the same dark terminal aesthetic with monospace font"
    - "Sidebar shows 5 nav items with correct icons and active state highlighting"
    - "SSE events from the server reach any client component that subscribes"
  artifacts:
    - path: "public/cortex.css"
      provides: "Complete CSS design system"
      contains: "--bg: #1a1a2e"
    - path: "src/app/layout.tsx"
      provides: "Root layout with sidebar and cortex.css link"
      contains: "cortex.css"
    - path: "src/components/Sidebar.tsx"
      provides: "Navigation sidebar with 5 items"
      contains: "use client"
    - path: "src/components/useSSE.ts"
      provides: "SSE consumer hook for real-time updates"
      contains: "EventSource"
  key_links:
    - from: "src/app/layout.tsx"
      to: "public/cortex.css"
      via: "link stylesheet"
      pattern: "cortex\\.css"
    - from: "src/app/layout.tsx"
      to: "src/components/Sidebar.tsx"
      via: "import and render"
      pattern: "Sidebar"
    - from: "src/components/useSSE.ts"
      to: "/api/sse"
      via: "EventSource connection"
      pattern: "new EventSource"
---

<objective>
Build the CSS design system, app layout with sidebar navigation, and the SSE client hook that all subsequent plans depend on.

Purpose: Every page and every real-time update in the Cortex UI depends on these three foundations. Without them, no other plan can produce visible output.
Output: `public/cortex.css` (complete design system), `src/app/layout.tsx` (root layout with sidebar), `src/components/Sidebar.tsx` (nav component), `src/components/useSSE.ts` (SSE hook).
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cortex-ui-tamir-interface/03-CONTEXT.md
@.planning/phases/03-cortex-ui-tamir-interface/03-UI-SPEC.md
@docs/12_VISUAL_GUIDELINES.md

<interfaces>
<!-- SSE endpoint format (from src/app/api/sse/route.ts) -->
<!-- SSE emits named events: event: ${event.type}\ndata: ${JSON.stringify(event.data)}\n\n -->
<!-- Event types: task:transition, task:buildlog, task:heartbeat, task:review, hire:requested, agent:invoked -->
<!-- The eventBus wraps all .emit(type, data) into { type, data } envelopes on the 'event' channel -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create complete CSS design system in public/cortex.css</name>
  <files>public/cortex.css</files>
  <read_first>
    - docs/12_VISUAL_GUIDELINES.md (MANDATORY — single source of truth for all CSS)
    - .planning/phases/03-cortex-ui-tamir-interface/03-UI-SPEC.md (spacing adjustments from UI checker)
  </read_first>
  <action>
Create `public/cortex.css` containing the complete design system from Doc 12. Copy all CSS verbatim from Doc 12 (per D-01, D-02). The file must contain, in order:

1. **CSS Variables** (:root block):
   - `--bg: #1a1a2e`, `--bg-2: #16213e`, `--bg-3: #0f3460`
   - `--border: #2a2a4a`
   - `--text: #e0e0e0`, `--text-dim: #a0a0b0`
   - `--accent: #e94560`, `--accent-dim: #c73652`
   - `--green: #00d68f`, `--amber: #ffb347`, `--red: #e94560`, `--gray: #666`
   - `--font: 'JetBrains Mono', 'Fira Code', 'Monaco', 'Courier New', monospace`
   - `--radius: 4px`

2. **Reset and base styles**:
   - `*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }`
   - `body { background: var(--bg); color: var(--text); font-family: var(--font); font-size: 13px; line-height: 1.5; }`
   - `a { color: var(--accent); text-decoration: none; }`
   - `h1 { font-size: 16px; font-weight: bold; color: var(--text); }`

3. **App layout** (sidebar + main content):
   - `.app-layout { display: flex; min-height: 100vh; }`
   - `.sidebar { width: 200px; background: var(--bg-2); border-right: 1px solid var(--border); padding: 16px; display: flex; flex-direction: column; position: fixed; top: 0; bottom: 0; left: 0; }`
   - `.main { margin-left: 200px; flex: 1; padding: 24px; min-height: 100vh; }`

4. **Sidebar nav**:
   - `.sidebar-logo { font-size: 15px; font-weight: bold; color: var(--accent); margin-bottom: 4px; }`
   - `.sidebar-version { font-size: 11px; color: var(--text-dim); margin-bottom: 24px; }`
   - `.nav-link { display: flex; align-items: center; gap: 10px; padding: 8px 12px; color: var(--text-dim); font-size: 13px; border-radius: var(--radius); border-left: 3px solid transparent; transition: all 0.15s; }`
   - `.nav-link:hover { color: var(--text); }`
   - `.nav-link.active { color: var(--text); border-left-color: var(--accent); }`
   - `.nav-icon { font-size: 16px; width: 20px; text-align: center; }`

5. **Badges** — copy verbatim from Doc 12:
   - Base: `.badge { display: inline-block; padding: 2px 7px; border-radius: 10px; font-size: 10px; font-weight: bold; text-transform: uppercase; letter-spacing: 0.05em; }`
   - `.badge-tech`, `.badge-marketing`, `.badge-ops` (dept colors at 15% opacity bg)
   - `.badge-active`, `.badge-pending`, `.badge-error`, `.badge-done` (status with borders)

6. **Buttons** — copy verbatim from Doc 12:
   - `.btn`, `.btn:hover`, `.btn-accent`, `.btn-accent:hover`, `.btn-sm`, `.btn-approve`, `.btn-approve:hover`, `.btn-cancel`, `.btn-cancel:hover`, `.btn:disabled`

7. **Cards and panels** — copy from Doc 12:
   - `.card`, `.card-title`, `.agent-card::before` with dept color variants

8. **Chat bubbles** — copy from Doc 12:
   - `.msg.agent .bubble`, `.msg.user .bubble`, `.ws-bubble`
   - `.input-area`, `textarea.chat-input`, `textarea.chat-input:focus`, `.send-btn`, `.send-btn:hover`

9. **Markdown rendering** — copy from Doc 12:
   - `.md-render` with all h1/h2/h3/code/pre/table/th/td/blockquote/a/img/strong rules

10. **Build log entries** — copy from Doc 12:
    - `.log-entry`, `.log-entry-header`, `.log-entry-header:hover`, `.log-entry-body`, `.log-entry.open .log-entry-body`
    - Type badges: `.log-type-think`, `.log-type-tool`, `.log-type-skill`, `.log-type-route`, `.log-type-complete`

11. **Tables** — copy from Doc 12:
    - `table`, `th`, `td`, `tr:hover td`

12. **Status dots** — copy from Doc 12:
    - `.dot`, `.dot-green`, `.dot-amber`, `.dot-red`, `.dot-gray`

13. **Form inputs** — copy from Doc 12:
    - `input[type="text"]`, `input[type="number"]`, `.search-bar`, `input:focus`, `.search-bar:focus`
    - `input[type="range"]` with thumb styling
    - `input[type="checkbox"]`

14. **Gallery cards** — copy from Doc 12:
    - `.gallery-card`, `.gallery-card:hover`, `.gallery-card.selected`, `.gallery-card.other-dept`
    - `.gallery-card-icon`, `.gallery-card-name`, `.gallery-card-desc`
    - Gallery grid: `.gallery-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(190px, 1fr)); gap: 10px; max-height: 300px; overflow-y: auto; }`

15. **Tabs** — copy from Doc 12:
    - `.tab-bar`, `.tab`, `.tab.active`, `.tab:hover`

16. **Stat row** (dashboard):
    - `.stat-row { display: flex; gap: 16px; margin-bottom: 24px; }`
    - `.stat-card { flex: 1; background: var(--bg-2); border: 1px solid var(--border); border-radius: var(--radius); padding: 16px; }`
    - `.stat-value { font-size: 24px; font-weight: bold; color: var(--accent); }`
    - `.stat-label { font-size: 10px; color: var(--text-dim); text-transform: uppercase; margin-top: 4px; }`

17. **Grid layouts**:
    - `.grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }`
    - `.grid-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 16px; }`

18. **Split pane** (Tamir page):
    - `.split-pane { display: flex; height: calc(100vh - 48px); }`
    - `.split-chat { width: 40%; border-right: 1px solid var(--border); display: flex; flex-direction: column; }`
    - `.split-canvas { width: 60%; display: flex; flex-direction: column; overflow-y: auto; padding: 24px; }`

19. **Avatar** — copy from Doc 12:
    - `.avatar { width: 30px; height: 30px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: bold; color: white; flex-shrink: 0; }`

20. **Scrollbars** — copy from Doc 12:
    - `::-webkit-scrollbar`, `::-webkit-scrollbar-track`, `::-webkit-scrollbar-thumb`, `::-webkit-scrollbar-thumb:hover`

21. **Animations** (per D-03 — ONLY these two):
    - `@keyframes fadeUp { from { opacity:0; transform:translateY(6px) } to { opacity:1; transform:translateY(0) } }`
    - `@keyframes blink { 0%,100% { opacity:1 } 50% { opacity:0 } }`
    - `.msg { animation: fadeUp 0.2s ease; }`

22. **Mobile breakpoint** (768px):
    - `.sidebar { display: none; }`, `.main { margin-left: 0; }`, `.grid-3, .grid-2 { grid-template-columns: 1fr; }`, `.stat-row { flex-direction: column; }`

23. **Utility classes**:
    - `.text-dim { color: var(--text-dim); }`, `.text-accent { color: var(--accent); }`
    - `.empty-state { text-align: center; padding: 48px 24px; color: var(--text-dim); }`
    - `.empty-state h3 { color: var(--text); margin-bottom: 8px; font-size: 13px; }`
    - `.empty-state p { font-size: 12px; }`
  </action>
  <verify>
    <automated>test -f public/cortex.css && grep -c "var(--" public/cortex.css | xargs -I {} test {} -gt 50 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - public/cortex.css exists and is a single file
    - Contains `:root` block with all 13 CSS variables: `--bg`, `--bg-2`, `--bg-3`, `--border`, `--text`, `--text-dim`, `--accent`, `--accent-dim`, `--green`, `--amber`, `--red`, `--gray`, `--font`, `--radius`
    - Contains `.badge-tech`, `.badge-marketing`, `.badge-ops`, `.badge-active`, `.badge-pending`, `.badge-error`, `.badge-done`
    - Contains `.btn`, `.btn-accent`, `.btn-sm`, `.btn-approve`, `.btn-cancel`
    - Contains `.card`, `.card-title`, `.agent-card`
    - Contains `.msg.agent .bubble`, `.msg.user .bubble`
    - Contains `.md-render` with h1, h2, h3, code, pre, table sub-rules
    - Contains `.log-entry`, `.log-type-think`, `.log-type-tool`, `.log-type-skill`, `.log-type-route`, `.log-type-complete`
    - Contains `.gallery-card`, `.gallery-card.selected`, `.gallery-card.other-dept`
    - Contains `.tab-bar`, `.tab`, `.tab.active`
    - Contains `.stat-row`, `.stat-card`, `.stat-value`, `.stat-label`
    - Contains `.split-pane`, `.split-chat`, `.split-canvas`
    - Contains `.avatar`
    - Contains `@keyframes fadeUp` and `@keyframes blink`
    - Contains `@media (max-width: 768px)` with `.sidebar { display: none; }`
    - Contains NO `@import` from external CSS frameworks
    - Contains NO Tailwind classes
  </acceptance_criteria>
  <done>Complete CSS design system in a single file with all Doc 12 components and UI-SPEC spacing adjustments</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app layout with sidebar navigation</name>
  <files>src/app/layout.tsx, src/components/Sidebar.tsx</files>
  <read_first>
    - src/app/layout.tsx (current stub — needs full rewrite)
    - docs/12_VISUAL_GUIDELINES.md (sidebar nav icons and styling)
    - .planning/phases/03-cortex-ui-tamir-interface/03-UI-SPEC.md (navigation structure table)
    - .planning/phases/03-cortex-ui-tamir-interface/03-CONTEXT.md (D-13, D-14)
  </read_first>
  <action>
**Sidebar.tsx** (`src/components/Sidebar.tsx`): Create a Client Component ('use client') with:
- Logo section: `<div className="sidebar-logo">Myelin</div>` and `<div className="sidebar-version">v10 — The Cortex</div>` (per D-14)
- Nav links array (per D-14 order):
  ```
  { href: '/', label: 'Dashboard', icon: '\u25A3' }
  { href: '/tamir', label: 'Tamir', icon: '\u25D0' }
  { href: '/deliverables', label: 'Deliverables', icon: '\u25CE' }
  { href: '/org-context', label: 'Org Context', icon: '\u25C6' }
  { href: '/vault', label: 'Vault', icon: '\u2B21' }
  ```
- Import `usePathname` from `next/navigation` for active state detection
- Active state: `className={pathname === link.href ? 'nav-link active' : 'nav-link'}` (for Dashboard, exact match; for others, `pathname.startsWith(link.href)` when href is not `/`)
- Each link rendered as `<Link>` from `next/link` with: `<span className="nav-icon">{icon}</span> {label}`

**layout.tsx** (`src/app/layout.tsx`): Full rewrite:
- Keep existing metadata: `title: 'Myelin v10 — The Cortex'`, `description: 'AI Company Operating System'`
- Add `<link rel="stylesheet" href="/cortex.css" />` in the `<head>` tag
- Body structure: `<div className="app-layout"><Sidebar /><main className="main">{children}</main></div>`
- Import Sidebar from `@/components/Sidebar`
- The `<html>` tag: `<html lang="en">`
- No global state providers — per project constraints (no state management libs)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20 || echo "TypeCheck done"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Sidebar.tsx exists with `'use client'` directive
    - Sidebar.tsx contains `usePathname` from `next/navigation`
    - Sidebar.tsx contains all 5 nav items with exact Unicode icons: \u25A3, \u25D0, \u25CE, \u25C6, \u2B21
    - Sidebar.tsx contains hrefs: `/`, `/tamir`, `/deliverables`, `/org-context`, `/vault`
    - Sidebar.tsx contains `nav-link active` class logic
    - src/app/layout.tsx contains `<link rel="stylesheet" href="/cortex.css" />`
    - layout.tsx contains `<Sidebar />` import and usage
    - layout.tsx contains `className="app-layout"`
    - layout.tsx contains `className="main"`
    - layout.tsx does NOT import Tailwind, Bootstrap, or any CSS framework
  </acceptance_criteria>
  <done>Root layout renders sidebar with 5 navigation items, active states, and cortex.css linked</done>
</task>

<task type="auto">
  <name>Task 3: Create SSE consumer hook</name>
  <files>src/components/useSSE.ts</files>
  <read_first>
    - src/app/api/sse/route.ts (SSE endpoint — understand event format: named events via `event: ${type}\ndata: ...`)
    - src/lib/events.ts (eventBus envelope format: { type, data })
    - .planning/phases/03-cortex-ui-tamir-interface/03-RESEARCH.md (SSE Client Hook code example and Pitfall 4)
  </read_first>
  <action>
Create `src/components/useSSE.ts` as a Client-side custom hook ('use client' at top):

```typescript
'use client';
import { useEffect, useRef } from 'react';

type SSEHandler = (data: Record<string, unknown>) => void;

export function useSSE(handlers: Record<string, SSEHandler>) {
  const handlersRef = useRef(handlers);
  handlersRef.current = handlers;

  useEffect(() => {
    const es = new EventSource('/api/sse');
    const eventTypes = Object.keys(handlersRef.current);
    const listeners: Array<[string, EventListener]> = [];

    for (const type of eventTypes) {
      const listener = (evt: Event) => {
        try {
          const data = JSON.parse((evt as MessageEvent).data);
          handlersRef.current[type]?.(data);
        } catch { /* ignore parse errors */ }
      };
      es.addEventListener(type, listener);
      listeners.push([type, listener]);
    }

    // EventSource auto-reconnects per spec; explicit 3s retry as fallback
    let reconnectTimeout: ReturnType<typeof setTimeout>;
    es.onerror = () => {
      reconnectTimeout = setTimeout(() => {
        // EventSource handles reconnection natively
      }, 3000);
    };

    return () => {
      clearTimeout(reconnectTimeout);
      for (const [type, listener] of listeners) {
        es.removeEventListener(type, listener);
      }
      es.close();
    };
  }, []); // handlers must be stable refs — caller wraps in useCallback if needed
}
```

IMPORTANT: The existing SSE endpoint (`src/app/api/sse/route.ts`) emits **named events** via `event: ${event.type}\n`. So the client MUST use `es.addEventListener('task:transition', ...)` — NOT `es.onmessage`. The `onmessage` handler only fires for events without a named type.

The hook takes a record of event type names to handler functions. Components use it like:
```typescript
useSSE({
  'task:transition': (data) => { /* update state */ },
  'task:heartbeat': (data) => { /* update heartbeat */ },
});
```
  </action>
  <verify>
    <automated>grep -c "EventSource" src/components/useSSE.ts | xargs -I {} test {} -ge 1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/useSSE.ts exists with `'use client'` directive
    - Contains `export function useSSE`
    - Contains `new EventSource('/api/sse')`
    - Uses `es.addEventListener(type, listener)` pattern (NOT `es.onmessage`)
    - Contains cleanup: `es.removeEventListener` and `es.close()` in return function
    - Uses `useRef` for handler stability
    - Contains `useEffect` with `[]` dependency array
  </acceptance_criteria>
  <done>SSE consumer hook connects to /api/sse, listens to named events, cleans up on unmount</done>
</task>

</tasks>

<verification>
1. `pnpm dev` starts without errors
2. Navigating to `localhost:3000` shows the sidebar with 5 nav items on a dark background
3. Clicking nav items updates the active state (accent left border)
4. Browser dev tools confirm `cortex.css` is loaded with all CSS variables
</verification>

<success_criteria>
- Complete design system in a single CSS file with all Doc 12 components
- Root layout renders sidebar navigation on all pages
- SSE hook available for real-time update consumption
- No Tailwind, no Bootstrap, no external CSS frameworks
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-cortex-ui-tamir-interface/03-01-SUMMARY.md`
</output>
