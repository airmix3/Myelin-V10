---
phase: 7
slug: assets-managment-system
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-30
---

# Phase 7 -- UI Design Contract

> Visual and interaction contract for the Assets Management System. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom CSS variables in `public/cortex.css`) |
| Preset | not applicable |
| Component library | none (hand-written components) |
| Icon library | Unicode symbols (consistent with sidebar pattern) |
| Font | `'JetBrains Mono', 'Fira Code', 'Monaco', 'Courier New', monospace` via `var(--font)` |

**Source:** Existing `cortex.css` design system, CLAUDE.md constraint ("Custom CSS variables only -- no Tailwind, no Bootstrap, no CSS frameworks").

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, badge internal spacing |
| sm | 8px | Compact element spacing, panel internal padding |
| md | 16px | Default element spacing, sidebar padding, card padding |
| lg | 24px | Section padding, detail panel section gaps |
| xl | 32px | Layout gaps between major panels |
| 2xl | 48px | City canvas margin from viewport edges |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Canvas hit target for asset buildings: 44px minimum touch/click target for interactive elements
- Detail panel width: 360px fixed (consistent with existing overlay patterns)

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 13px | 400 (regular) | 1.5 |
| Label | 11px | 700 (bold) | 1.4 |
| Heading | 16px | 700 (bold) | 1.2 |
| Display | 20px | 700 (bold) | 1.2 |

**Weight rationale:** Two weights only -- 400 (regular) for body text and 700 (bold) for labels, headings, and display text. Labels remain visually subordinate to headings due to smaller size (11px vs 16px/20px) despite sharing the same weight.

**Source:** Existing `cortex.css` -- body is 13px/1.5, h1 is 16px/bold. Display size (20px) added for asset name in detail panel and city district labels.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#1a1a2e` (`var(--bg)`) | Canvas background, page background |
| Secondary (30%) | `#16213e` (`var(--bg-2)`) | Detail panel background, evolution timeline bar, sidebar |
| Accent (10%) | `#e94560` (`var(--accent)`) | Primary CTA buttons, selected asset highlight ring, active nav state |
| Destructive | `#ff3b5c` (`var(--red)`) | Remove asset confirmation, dependency break warnings |

### Return Factor Glow Colors (per D-26, D-27)

| Factor | Color | CSS Variable |
|--------|-------|-------------|
| Revenue generation | `#ffb347` (gold/amber) | `var(--amber)` |
| Competitive moat | `#6496ff` (blue) | `--asset-moat: #6496ff` |
| Core technology | `#00d68f` (green) | `var(--green)` |
| Brand equity | `#a855f6` (purple) | `--asset-brand: #a855f6` |

### Asset Maturity Visual Mapping (per D-09, D-18)

| Maturity | Building Scale | Visual Treatment |
|----------|---------------|-----------------|
| nascent | Small foundation outline (20px tall) | Wireframe/translucent, faint glow |
| developing | Small structure (32px tall) | Partial opacity (0.7), growing glow |
| established | Medium building (48px tall) | Full opacity, steady glow |
| foundational | Tall landmark (64px tall) | Full opacity, bright aura, subtle pulse |
| legacy | Medium-tall (56px tall) | Slightly dimmed (0.8 opacity), warm tint |
| heritage | Large monument (72px tall) | Golden tint overlay, heritage shimmer |

### Health Degradation Visuals (per D-32)

| Health State | Visual Effect |
|-------------|--------------|
| Healthy | Normal rendering with return factor glow |
| Stale (no activity > threshold) | Reduced glow intensity (50%), slight desaturation |
| Degraded | Cracks overlay (canvas line pattern), glow flickers |
| Critical | Dim building (0.4 opacity), red-tinted glow, pulsing alert |

Accent reserved for: "Create Asset" button, selected asset highlight ring in city view, active sidebar nav item, primary action buttons in detail panel.

---

## New CSS Variables

Add to `cortex.css`:

```css
/* Asset system colors */
--asset-moat:    #6496ff;
--asset-brand:   #a855f6;
--asset-revenue: var(--amber);
--asset-tech:    var(--green);
--red:           #ff3b5c;

/* Asset detail panel */
--panel-width: 360px;
```

---

## Page Architecture

### /assets Page (per D-18, D-19, D-20, D-21, D-35)

**Layout:** Full-bleed canvas with floating UI overlays. No standard page padding.

| Zone | Position | Content |
|------|----------|---------|
| City Canvas | Full page area | Canvas-rendered interactive city map (pannable, zoomable) |
| Toolbar | Top-right, floating | "Create Asset" button, zoom controls (with aria-label tooltips), filter toggles by category |
| Detail Panel | Right side, 360px, slides in on asset click | Past/Present/Future tabs, steward info, health, linked tasks, inline chat |
| Evolution Timeline | Bottom, full width, 80px tall | Horizontal timeline with milestone markers; shows selected asset history or recent all-asset activity |
| District Labels | Overlaid on canvas | Category district names rendered on canvas (not DOM) |

**Canvas Pattern:** Follow `NeuralHero.tsx` architecture -- `useRef<HTMLCanvasElement>`, `requestAnimationFrame` loop, resize observer. Full custom Canvas2D drawing for buildings, districts, particles, and glow effects.

### Zoom Control Accessibility

| Control | aria-label | Tooltip text |
|---------|-----------|-------------|
| Zoom in button | "Zoom in" | "Zoom in" |
| Zoom out button | "Zoom out" | "Zoom out" |
| Reset zoom button | "Reset zoom to default" | "Reset zoom" |

### Asset Detail Panel (per D-21, D-22)

| Section | Content |
|---------|---------|
| Header | Asset name (20px bold), category badge, maturity badge, steward avatar |
| Tab: Past | Event timeline list -- born date, major changes, deliverables absorbed |
| Tab: Present | Health signals, current locations (canonical marked), return factors with glow dots, CEO annotations |
| Tab: Future | Linked pending tasks, steward recommendations, follow-up suggestions |
| Linked Tasks | Scrollable list of related tasks with status badges |
| Steward Chat | Inline chat input at panel bottom (same pattern as deliverable chat) |
| Actions | "Add Annotation", "Change Maturity", "Add Location", "View Dependencies" |

### Create Asset Form (per D-01, D-08, D-28)

| Field | Type | Notes |
|-------|------|-------|
| Name | Text input | Required, max 100 chars |
| Description | Textarea | Required, 3 rows |
| Category | Select dropdown | code, brand, IP, digital-product, knowledge |
| Steward | Select dropdown | CTO, CMO, COO (filtered by relevance) |
| Initial Maturity | Select dropdown | Default: nascent |
| Return Factors | Checkbox group | Revenue, Moat, Core tech, Brand equity (1-4 selections) |
| Primary Location | Text input | Optional, free-text path/URL |

Rendered as a modal overlay on the city canvas.

### Deliverable Page Additions (per D-03, D-07)

| Element | Position | Content |
|---------|----------|---------|
| Promotion Banner | Top of deliverable page, below metadata bar | "Steward recommends promoting to asset: {asset name}" with "Accept Promotion" / "Dismiss Recommendation" buttons |
| Promote Button | Action bar | "Promote to Asset" opens modal with steward dropdown + intent textarea |

---

## Component Inventory

### New Components

| Component | File | Type | Description |
|-----------|------|------|-------------|
| AssetCityCanvas | `src/app/assets/AssetCityCanvas.tsx` | Client | Full-page canvas with pan/zoom, building rendering, district clustering |
| AssetDetailPanel | `src/app/assets/AssetDetailPanel.tsx` | Client | Slide-in right panel with tabs, chat, actions |
| AssetToolbar | `src/app/assets/AssetToolbar.tsx` | Client | Floating top-right toolbar with create button and filters |
| EvolutionTimeline | `src/app/assets/EvolutionTimeline.tsx` | Client | Bottom horizontal timeline with milestone markers |
| CreateAssetModal | `src/app/assets/CreateAssetModal.tsx` | Client | Modal form for CEO asset creation |
| PromotionBanner | `src/app/deliverables/PromotionBanner.tsx` | Client | Deliverable page banner for steward promotion recommendation |
| PromoteToAssetModal | `src/app/deliverables/PromoteToAssetModal.tsx` | Client | Modal for CEO-initiated promotion with steward selection |

### Modified Components

| Component | Change |
|-----------|--------|
| `Sidebar.tsx` | Add "Assets" nav item: `{ href: '/assets', label: 'Assets', icon: '\u{1F3D7}' }` below Deliverables (or use `\u25A8` if emoji not preferred) |

---

## Interaction States

### City Canvas Interactions

| Interaction | Behavior |
|-------------|----------|
| Pan | Click-drag on empty canvas area moves viewport |
| Zoom | Mouse wheel / pinch zooms in/out (min 0.5x, max 3x) |
| Hover building | Building glow intensifies, tooltip shows asset name + maturity |
| Click building | Detail panel slides in from right (360px), evolution timeline shows asset history |
| Click empty area (while panel open) | Detail panel slides out, timeline reverts to all-asset view |
| Double-click building | Focus zoom to building (animate pan + zoom to center it) |

### Detail Panel States

| State | Visual |
|-------|--------|
| Loading | Skeleton placeholder (3 shimmer bars) |
| Loaded | Full content with tabs |
| Chat active | Input focused, typing indicator when steward responding |
| Empty past | "No history yet" with muted text |

### Create Asset Modal States

| State | Visual |
|-------|--------|
| Default | Form with all fields, "Create Asset" primary button disabled until required fields filled |
| Submitting | Button shows "Creating..." with disabled state |
| Success | Modal closes, new building appears on canvas with brief glow animation |
| Error | Inline error message below form in `var(--red)` |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Create Asset" |
| Sidebar label | "Assets" |
| Empty state heading | "Your company portfolio is empty" |
| Empty state body | "Create your first asset to start building your company map. Assets are the things your company manages over time -- code, brand, IP, products, and knowledge." |
| Empty state CTA | "Create First Asset" |
| Error state (load failure) | "Could not load assets. Check that the server is running and refresh the page." |
| Error state (create failure) | "Failed to create asset. Please try again." |
| Promotion banner | "{Steward name} recommends promoting this deliverable to a company asset" |
| Promotion banner accept | "Accept Promotion" |
| Promotion banner dismiss | "Dismiss Recommendation" |
| Promote button | "Promote to Asset" |
| Promote modal heading | "Promote Deliverable to Asset" |
| Promote modal intent label | "How do you see this asset? (optional)" |
| Promote modal intent placeholder | "Describe your vision for this asset..." |
| Promote modal CTA | "Confirm Promotion" |
| Annotation input placeholder | "Add a note about this asset..." |
| Steward chat placeholder | "Ask the steward about this asset..." |
| Detail panel empty past | "No history recorded yet" |
| Detail panel empty future | "No planned work for this asset" |
| Maturity change confirmation | "Change maturity to {level}?" |
| Remove asset confirmation | "Remove Asset": "This will archive the asset and its history. Existing files will not be deleted. Continue?" |
| Dependency link label | "Add Dependency" |
| Dependency ripple warning | "Changing {asset name} may affect {N} dependent assets" |
| Health: stale | "No activity in {N} days" |
| Health: healthy | "Active and maintained" |
| Canvas tooltip | "{asset name} -- {maturity}" |
| District labels | "Tech District", "Media District", "IP District", "Product District", "Knowledge District" |
| Zoom in tooltip | "Zoom in" |
| Zoom out tooltip | "Zoom out" |
| Reset zoom tooltip | "Reset zoom" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (project does not use shadcn) |

No third-party registries. All components are hand-written following existing `cortex.css` design system patterns.

---

## Canvas Rendering Notes

### District Layout (Claude's Discretion per D-18, D-19)

Each asset category occupies a distinct district on the canvas. Districts are loosely clustered zones (not rigid grids). New assets appear in their category's district with slight random offset for organic feel.

| Category | District Position | Visual Style |
|----------|------------------|--------------|
| code | Center-left | Circuit-board patterns, terminal-green accent lines |
| brand | Top-right | Smooth curves, gradient fills, purple/pink tones |
| IP | Bottom-right | Shield/vault motifs, structured geometric shapes |
| digital-product | Top-center | Modern glass-building style, blue/cyan highlights |
| knowledge | Bottom-left | Library/book-stack motifs, warm amber tones |

### Building Rendering Pipeline

1. Clear canvas with `var(--bg)` fill
2. Draw district boundary zones (subtle dashed borders at 0.1 opacity)
3. Draw district labels (11px bold, `var(--text-dim)`)
4. For each asset: draw building shape scaled by maturity, apply return factor glow, apply health degradation if applicable
5. Draw dependency lines between connected assets (dashed, `var(--border)` color, 0.3 opacity)
6. Draw hover/selection highlights on top layer
7. Draw floating particles for recently active assets (similar to NeuralHero firing pattern)

### Canvas Layout Constants

These values are canvas layout constants, not spacing tokens. They are not part of the spacing scale.

| Constant | Value | Justification |
|----------|-------|---------------|
| Evolution timeline milestone spacing | 120px horizontal | Canvas layout constant, not a spacing token -- set to 120px to prevent milestone label overlap at default zoom level |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
