---
phase: quick-260329-0g7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/server.ts
  - src/lib/mcp/access-control.ts
  - src/lib/invoke-agent.ts
  - src/lib/workspace.ts
  - src/lib/seed-gallery.ts
  - src/lib/db.ts
  - src/lib/config.ts
  - src/instrumentation.ts
  - src/components/Sidebar.tsx
  - src/app/layout.tsx
  - src/app/settings/page.tsx
  - src/app/api/system/info/route.ts
  - src/app/api/hire_requests/[id]/approve/route.ts
  - public/cortex.css
  - data/CLAUDE.md
  - data/.claude/settings.json
  - data/departments/cos/.claude/settings.json
  - data/departments/cos/manager-desk/.claude/settings.json
  - data/departments/cos/planning-desk/.claude/settings.json
  - data/departments/operations/manager-desk/.claude/settings.json
  - data/departments/operations/planning-desk/.claude/settings.json
  - data/departments/marketing/manager-desk/.claude/settings.json
  - data/departments/marketing/planning-desk/.claude/settings.json
  - data/departments/tech/manager-desk/.claude/settings.json
  - data/departments/tech/planning-desk/.claude/settings.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "All MCP tool references use mcp__cortex__ prefix instead of mcp__myelin__"
    - "UI shows 'Cortex' as the product name, not 'Myelin'"
    - "Code variables and function names reference 'cortex' not 'myelin' for product identity"
    - "Agent settings.json files allow mcp__cortex__* tools"
    - "Company references to 'Myelin' (the neurotech company) are preserved unchanged"
  artifacts:
    - path: "src/lib/mcp/server.ts"
      provides: "buildCortexMcpServer function"
      contains: "name: 'cortex'"
    - path: "src/lib/mcp/access-control.ts"
      provides: "Access control with mcp__cortex__ prefix"
      contains: "mcp__cortex__"
    - path: "data/CLAUDE.md"
      provides: "Agent workspace intro referencing Cortex"
      contains: "Cortex"
  key_links:
    - from: "src/lib/invoke-agent.ts"
      to: "src/lib/mcp/server.ts"
      via: "import buildCortexMcpServer"
      pattern: "buildCortexMcpServer"
    - from: "src/lib/workspace.ts"
      to: "SDK allowedTools"
      via: "mcp__cortex__* pattern"
      pattern: "mcp__cortex__"
---

<objective>
Rename the product/system identity from "Myelin" to "Cortex" across all code, UI, MCP tool prefixes, and agent configuration files.

Purpose: "Myelin" is the company name (neurotech startup). "Cortex" is the AIOS product name. This rename clarifies the distinction so the product (The Cortex) has its own identity separate from the company brand.

Output: All product-level references updated; company-level references preserved.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename MCP tool prefix and code references in src/</name>
  <files>
    src/lib/mcp/server.ts
    src/lib/mcp/access-control.ts
    src/lib/invoke-agent.ts
    src/lib/workspace.ts
    src/lib/seed-gallery.ts
    src/lib/config.ts
    src/lib/db.ts
    src/instrumentation.ts
    src/components/Sidebar.tsx
    src/app/layout.tsx
    src/app/settings/page.tsx
    src/app/api/system/info/route.ts
    src/app/api/hire_requests/[id]/approve/route.ts
    public/cortex.css
  </files>
  <action>
Rename all PRODUCT/SYSTEM references from "Myelin"/"myelin" to "Cortex"/"cortex" in source code. Specifically:

**MCP server and tools (critical path):**
- `src/lib/mcp/server.ts`: Rename `buildMyelinMcpServer` to `buildCortexMcpServer`, change `name: 'myelin'` to `name: 'cortex'`
- `src/lib/mcp/access-control.ts`: Replace all `mcp__myelin__` with `mcp__cortex__` (TAMIR_ONLY, DEPT_HEAD_ONLY, ALL_AGENTS arrays, and the `startsWith('mcp__myelin__')` check). Also update the comment on line 5.
- `src/lib/invoke-agent.ts`: Update import `buildMyelinMcpServer` to `buildCortexMcpServer`, update variable `myelinServer` to `cortexServer`, update `mcpServers: { myelin: myelinServer }` to `mcpServers: { cortex: cortexServer }`, update `allowedTools: ['mcp__myelin__*']` to `allowedTools: ['mcp__cortex__*']`
- `src/lib/workspace.ts`: Replace all 4 occurrences of `mcp__myelin__*` with `mcp__cortex__*` in the allow arrays
- `src/app/api/hire_requests/[id]/approve/route.ts`: Replace all `mcp__myelin__` with `mcp__cortex__` in the allowedTools array

**Gallery seed:**
- `src/lib/seed-gallery.ts`: Change `name: 'myelin'` to `name: 'cortex'`, update description from "Myelin internal tools" to "Cortex internal tools", update findUnique where clause, update log messages

**UI text:**
- `src/components/Sidebar.tsx`: Change "Myelin" to "Cortex" in the sidebar logo div
- `src/app/layout.tsx`: Change title from `'Myelin v10 — The Cortex'` to `'Cortex — AI Operating System'`

**Infrastructure:**
- `src/instrumentation.ts`: Rename `__myelinInit` to `__cortexInit` (3 occurrences), change log messages from "Myelin v10 server initialization" to "Cortex server initialization". Keep the vault seed title "Myelin Company DNA" as-is (that is company identity, not product).
- `src/lib/config.ts`: Update comment from "Myelin runtime" to "Cortex runtime"
- `public/cortex.css`: Update comment from "Myelin v10 -- The Cortex" to "Cortex -- AI Operating System"

**Do NOT change:**
- `src/lib/db.ts` DB_PATH `myelin.db` — the database filename is infrastructure, not user-facing. Renaming it would break existing databases.
- `src/app/settings/page.tsx` and `src/app/api/system/info/route.ts` — these show the actual db file path `data/myelin.db` which is a fact, not branding.
- Any soul.md files — those reference "Myelin" as the company name.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && grep -rn "mcp__myelin" src/ | grep -v node_modules | grep -v ".next" && echo "FAIL: mcp__myelin still found" || echo "PASS: no mcp__myelin in src/"</automated>
    <automated>cd /home/omersh/myelin-gsd && grep -rn "buildMyelinMcpServer" src/ && echo "FAIL: old function name found" || echo "PASS: function renamed"</automated>
    <automated>cd /home/omersh/myelin-gsd && grep -rn "__myelinInit" src/ && echo "FAIL: old init guard found" || echo "PASS: init guard renamed"</automated>
  </verify>
  <done>All MCP tool prefixes use mcp__cortex__, all product UI text says "Cortex", all code identifiers use cortex/Cortex naming. Company references in soul.md files unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update agent workspace configs and data/ CLAUDE.md</name>
  <files>
    data/CLAUDE.md
    data/.claude/settings.json
    data/departments/cos/.claude/settings.json
    data/departments/cos/manager-desk/.claude/settings.json
    data/departments/cos/planning-desk/.claude/settings.json
    data/departments/operations/manager-desk/.claude/settings.json
    data/departments/operations/planning-desk/.claude/settings.json
    data/departments/marketing/manager-desk/.claude/settings.json
    data/departments/marketing/planning-desk/.claude/settings.json
    data/departments/tech/manager-desk/.claude/settings.json
    data/departments/tech/planning-desk/.claude/settings.json
  </files>
  <action>
Update all agent-facing configuration files to use the new "Cortex" product name and `mcp__cortex__*` tool prefix.

**data/CLAUDE.md** — This is the agent project boundary file:
- Change heading from "# Myelin v10 Agent Workspace" to "# Cortex Agent Workspace"
- Change "agent execution environment for Myelin v10 — The Cortex" to "agent execution environment for Cortex — AI Operating System"
- Keep "You are an AI agent employee of Myelin" as-is (company name)
- Change `mcp__myelin__*` to `mcp__cortex__*` in the MCP tools reference

**data/.claude/settings.json** — Root agent settings:
- Change `mcp__myelin__*` to `mcp__cortex__*` in the allow array

**All department settings.json files** (9 files across cos, operations, marketing, tech — both manager-desk and planning-desk):
- Change `mcp__myelin__*` to `mcp__cortex__*` in each allow array

Use a systematic find-and-replace approach. Every settings.json file under data/departments/*/[manager-desk|planning-desk]/.claude/ needs this change, plus the cos/.claude/settings.json at dept level.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && grep -rn "mcp__myelin" data/ && echo "FAIL: mcp__myelin still in data/" || echo "PASS: no mcp__myelin in data/"</automated>
    <automated>cd /home/omersh/myelin-gsd && grep -c "mcp__cortex" data/CLAUDE.md && echo "PASS: cortex reference in data/CLAUDE.md"</automated>
  </verify>
  <done>All agent workspace configs reference mcp__cortex__* tools. data/CLAUDE.md uses Cortex product name while preserving Myelin company name. Agents will see the correct tool prefix on next invocation.</done>
</task>

</tasks>

<verification>
- `grep -rn "mcp__myelin" src/ data/` returns zero results
- `grep -rn "buildMyelinMcpServer\|__myelinInit" src/` returns zero results
- `grep -rn "mcp__cortex" src/ data/` returns matches in all expected files
- Sidebar shows "Cortex" not "Myelin"
- Page title is "Cortex — AI Operating System"
- Soul.md files still reference "Myelin" as the company name (unchanged)
- Database file is still `myelin.db` (unchanged)
</verification>

<success_criteria>
All product/system identity references renamed from Myelin to Cortex. MCP tool prefix is mcp__cortex__* everywhere. UI shows Cortex. Company-name references to Myelin preserved. No runtime breakage from the rename.
</success_criteria>

<output>
After completion, create `.planning/quick/260329-0g7-rename-system-from-myelin-to-cortex-acro/260329-0g7-SUMMARY.md`
</output>
