---
phase: quick-260328-vh6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ConfigPanel.tsx
autonomous: true
requirements: [QUICK-vh6]
must_haves:
  truths:
    - "Planning screen no longer shows autonomy level slider"
    - "Config panel still saves budget, constraints, tools, and skills correctly"
  artifacts:
    - path: "src/components/ConfigPanel.tsx"
      provides: "Config panel without autonomy slider"
  key_links:
    - from: "src/components/ConfigPanel.tsx"
      to: "/api/tasks/[taskId]/config"
      via: "fetch PUT with config object"
      pattern: "fetch.*config"
---

<objective>
Remove the autonomy level slider from the planning config panel UI.

Purpose: The autonomy level control is not needed in the planning screen.
Output: Clean ConfigPanel.tsx without autonomy-related UI or state.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ConfigPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove autonomy level UI and state from ConfigPanel</name>
  <files>src/components/ConfigPanel.tsx</files>
  <action>
In src/components/ConfigPanel.tsx, remove the following:

1. Delete the AUTONOMY_LEVELS constant (line 13)
2. Delete the autonomyIndex state declaration (lines 16-18)
3. Delete the handleAutonomyChange function (lines 57-59)
4. In the saveConfig callback (line 27-34), remove the `autonomyLevel: AUTONOMY_LEVELS[autonomyIndex]` line from the config object
5. In the saveConfig useCallback deps array (line 44), remove `autonomyIndex`
6. Delete the entire autonomy slider UI block (lines 73-95, from the comment "Autonomy Level Slider" through the closing div)

Keep the TaskConfig type import — autonomyLevel stays in the type definition (used elsewhere). The config object sent to the API will simply omit autonomyLevel, which is fine since TaskConfig fields are used as Partial.

Do NOT touch: budget input, constraints textarea, GalleryPanel, or any other UI elements.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && grep -c "autonomy\|AUTONOMY" src/components/ConfigPanel.tsx | grep -q "^0$" && echo "PASS: no autonomy references" || echo "FAIL: autonomy references remain"</automated>
  </verify>
  <done>ConfigPanel.tsx has zero references to autonomy level — no constant, no state, no handler, no UI slider. Budget, constraints, and gallery still render normally.</done>
</task>

</tasks>

<verification>
- grep for "autonomy" in ConfigPanel.tsx returns zero matches
- No TypeScript compilation errors: cd /home/omersh/myelin-gsd && npx tsc --noEmit --pretty 2>&1 | head -20
</verification>

<success_criteria>
- Autonomy slider completely removed from planning config panel
- No dead code (constants, state, handlers) left behind
- Config panel still functions for budget, constraints, tools, and skills
</success_criteria>

<output>
After completion, create `.planning/quick/260328-vh6-remove-autonomy-level-bar-from-planning-/260328-vh6-SUMMARY.md`
</output>
