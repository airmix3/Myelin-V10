---
phase: quick
plan: 260328-l84
type: execute
wave: 1
depends_on: []
files_modified:
  - data/departments/cos/skills/installer/SKILL.md
  - src/lib/mcp/tools/install.ts
  - src/lib/workspace.ts
autonomous: true
requirements: [INSTALLER-SKILL, INSTALL-PROMPTS, SKILL-LOADING]
must_haves:
  truths:
    - "Tamir has an installer SKILL.md describing the full install procedure"
    - "install_tool and install_skill prompts pass rich context (requesting agent, task, justification, approval info)"
    - "Tamir's cos desk has cos/skills symlinked so the installer skill is loaded"
  artifacts:
    - path: "data/departments/cos/skills/installer/SKILL.md"
      provides: "Installer skill definition"
      contains: "PRE-INSTALL"
    - path: "src/lib/mcp/tools/install.ts"
      provides: "Enriched install prompts"
      contains: "requesting agent"
    - path: "src/lib/workspace.ts"
      provides: "cos skills symlink for Tamir desk"
      contains: "cosSkillsTarget"
  key_links:
    - from: "src/lib/mcp/tools/install.ts"
      to: "data/departments/cos/skills/installer/SKILL.md"
      via: "prompt references installer skill"
      pattern: "installer skill"
---

<objective>
Create a Tamir installer skill (SKILL.md) and enrich the install_tool/install_skill prompts in install.ts with full context so Tamir has clear procedure and rich information when performing installations. Also fix the missing cos skills symlink in Tamir's planning desk.

Purpose: Tamir currently receives barebones procedural prompts when installing tools/skills. The installer skill gives him a reusable procedure, and enriched prompts give him the context to make good decisions.
Output: SKILL.md file, updated install.ts prompts, workspace.ts symlink fix
</objective>

<context>
@data/departments/cos/skills/memory-management/SKILL.md (format reference)
@src/lib/mcp/tools/install.ts (prompts to enrich)
@src/lib/workspace.ts (symlink fix needed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create installer SKILL.md and symlink cos skills into Tamir's desk</name>
  <files>data/departments/cos/skills/installer/SKILL.md, src/lib/workspace.ts</files>
  <action>
Create `data/departments/cos/skills/installer/SKILL.md` following the existing cos skill format (YAML frontmatter with name/status/scope/version + markdown body). Content must include:

**Frontmatter:** name: installer, status: active, scope: global, version: "1.0"

**Body sections:**
1. **Goal** — You are the company's installer. When agents need tools or skills, their dept head approves and you execute the installation.

2. **When to Trigger** — You receive an installation prompt from the system (via install_tool or install_skill MCP tools). You never self-trigger.

3. **Context You Receive** — Document the full context template:
   - Requesting agent ID and their department
   - Task ID they're working on
   - Justification for the install
   - Department head who approved and their reasoning
   - Target directory path
   - Command to run

4. **PRE-INSTALL Checks:**
   - For tools: verify target directory exists, ensure package.json present (init if not)
   - For skills: verify target directory exists
   - Check the item name looks legitimate (no shell injection in names)

5. **Installation Procedure:**
   - For tools: `npx @smithery/cli mcp add <name> --client claude-code` in target dir
   - For skills: `npx skills add <owner/repo> --skill '<Display Name>' --yes` in target dir
   - Capture stdout and stderr

6. **POST-INSTALL Verification:**
   - For tools: exit code 0, check node_modules or package.json updated
   - For skills: exit code 0, SKILL.md file exists in new skill directory
   - If verification fails, report exact error output

7. **FAILURE Handling:**
   - Log the exact error message
   - Do NOT retry automatically — report failure with full error context
   - The calling system handles retry decisions

8. **NOTE:** Hot-reload happens automatically after you return — you do not handle it. Just install and report.

Also update `src/lib/workspace.ts` in `ensurePlanningDesks()` for the cos block (around line 182-241). After creating `skillsDir` at `cosDir/.claude/skills/`, add a symlink from `data/departments/cos/skills/` into `cosDir/.claude/skills/cos` — matching the same pattern used for other departments' planning desks (lines 259-263). This ensures Tamir's desk loads cos skills (including the new installer skill) via the SDK's skill discovery.
  </action>
  <verify>
    <automated>test -f data/departments/cos/skills/installer/SKILL.md && grep -q "PRE-INSTALL" data/departments/cos/skills/installer/SKILL.md && grep -q "cosSkillsSource\|cosSkillsTarget" src/lib/workspace.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Installer SKILL.md exists with all required sections (PRE-INSTALL, POST-INSTALL, FAILURE, context template). workspace.ts symlinks cos/skills into Tamir's .claude/skills/cos.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich install_tool and install_skill prompts with full context</name>
  <files>src/lib/mcp/tools/install.ts</files>
  <action>
Update both install prompts in `src/lib/mcp/tools/install.ts` to pass rich context to Tamir and reference the installer skill.

**For `installPrompt` (tool install, ~line 179):** Replace the current barebones prompt with:

```
You are performing a TOOL INSTALLATION. Follow your installer skill procedure.

## Context
- Requesting agent: ${ctx.agentId} (department: ${ctx.department})
- Task: ${ctx.taskId}
- Justification: ${justification}
- Approved by: ${getDeptHeadId(ctx.department)} (department head)
- Approval reasoning: ${approval.reasoning}

## Installation Details
- Package: ${package_name}
- Type: MCP tool server (Smithery)
- Target directory: ${deptToolsDir}
- Command: npx @smithery/cli mcp add ${package_name} --client claude-code

## Instructions
1. cd to the target directory
2. Ensure package.json exists (run npm init -y if not)
3. Run the install command
4. Verify success: exit code 0, package appears in package.json or node_modules
5. Report result clearly — success with what was installed, or failure with exact error
```

**For skill install prompt (~line 341):** Replace with equivalent context-rich prompt:

```
You are performing a SKILL INSTALLATION. Follow your installer skill procedure.

## Context
- Requesting agent: ${ctx.agentId} (department: ${ctx.department})
- Task: ${ctx.taskId}
- Justification: ${justification}
- Approved by: ${getDeptHeadId(ctx.department)} (department head)
- Approval reasoning: ${approval.reasoning}

## Installation Details
- Skill: ${skill_id} (display name: "${displayName}")
- Owner/Repo: ${ownerRepo}
- Target directory: ${deptSkillsDir}
- Command: npx skills add ${ownerRepo} --skill '${displayName}' --yes

## Instructions
1. cd to the target directory
2. Run the install command
3. Verify success: exit code 0, new directory with SKILL.md exists
4. Report result clearly — success with skill path, or failure with exact error
```

Note: `approval` is already in scope at the point where these prompts are constructed (approval happens before Tamir is invoked). The `getDeptHeadId` function is already defined in the file.
  </action>
  <verify>
    <automated>grep -c "Requesting agent" src/lib/mcp/tools/install.ts | grep -q "2" && grep -q "installer skill" src/lib/mcp/tools/install.ts && grep -q "approval.reasoning" src/lib/mcp/tools/install.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both installPrompt and skill install prompt contain: requesting agentId, taskId, justification, approving dept head ID, approval reasoning, target directory, and command. Both reference the installer skill.</done>
</task>

</tasks>

<verification>
1. `data/departments/cos/skills/installer/SKILL.md` exists with frontmatter and all sections
2. `src/lib/mcp/tools/install.ts` has enriched prompts with context fields for both tool and skill install
3. `src/lib/workspace.ts` symlinks cos/skills into Tamir's .claude/skills/ directory
4. `npx tsc --noEmit` passes (no type errors from prompt string changes)
</verification>

<success_criteria>
- Installer SKILL.md follows existing cos skill format with PRE-INSTALL, POST-INSTALL, FAILURE sections
- Both install prompts pass requesting agent, task, justification, approver, approval reasoning
- Tamir's cos desk gets cos/skills symlinked for skill discovery
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260328-l84-create-tamir-installer-skill-and-enrich-/260328-l84-SUMMARY.md`
</output>
