---
phase: quick-260328-c3k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/invoke-agent.ts
autonomous: true
must_haves:
  truths:
    - "Agents read their desk CLAUDE.md during execution"
    - "Agents load symlinked skills from desk/.claude/skills/"
    - "Agents respect data/CLAUDE.md project boundary (do NOT read GSD root CLAUDE.md)"
  artifacts:
    - path: "src/lib/invoke-agent.ts"
      provides: "Agent invocation with project settings enabled"
      contains: "settingSources: ['project']"
  key_links:
    - from: "src/lib/invoke-agent.ts"
      to: "data/workspaces/{taskId}/desk/CLAUDE.md"
      via: "settingSources: ['project'] + cwd: opts.deskDir"
      pattern: "settingSources.*project"
---

<objective>
Fix agents not loading skills and desk-level CLAUDE.md during execution.

Purpose: The `settingSources: []` in invoke-agent.ts prevents the Claude subprocess from reading ANY project settings, which means agents ignore their desk CLAUDE.md, symlinked skills, and MCP tool definitions. The data/CLAUDE.md project boundary already exists to prevent reading the GSD root CLAUDE.md, so enabling project settings is safe.

Output: One-line fix in invoke-agent.ts that unblocks all skill/tool loading.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/invoke-agent.ts
@data/CLAUDE.md
@data/.claude/settings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable project settings in agent invocation</name>
  <files>src/lib/invoke-agent.ts</files>
  <action>
    In `src/lib/invoke-agent.ts`, change line 197 from:
    ```
    settingSources: [],
    ```
    to:
    ```
    settingSources: ['project'],
    ```

    This is safe because:
    - Agent CWD is `data/workspaces/{taskId}/desk/` which has its own CLAUDE.md
    - `data/CLAUDE.md` exists as a project boundary, preventing upward traversal to the GSD root CLAUDE.md
    - `data/.claude/settings.json` exists with agent-appropriate permissions

    Do NOT change any other query options. This is a single-token fix.
  </action>
  <verify>
    <automated>grep -n "settingSources" src/lib/invoke-agent.ts | grep "project"</automated>
  </verify>
  <done>invoke-agent.ts passes settingSources: ['project'] so agents will read their desk CLAUDE.md and symlinked skills on next invocation</done>
</task>

<task type="auto">
  <name>Task 2: Verify project boundary files exist</name>
  <files>data/CLAUDE.md, data/.claude/settings.json</files>
  <action>
    Verify that the two project boundary files exist and are non-empty. These are critical for preventing agents from reading the GSD root CLAUDE.md:

    1. `data/CLAUDE.md` — must exist (creates project boundary so Claude stops walking up directories)
    2. `data/.claude/settings.json` — must exist (agent permissions)

    If either is missing, this fix would be UNSAFE (agents would read the root CLAUDE.md). In that case, recreate them per the patterns from Phase quick-260328-188.

    Do NOT modify these files if they already exist and have content.
  </action>
  <verify>
    <automated>test -s data/CLAUDE.md && test -s data/.claude/settings.json && echo "PASS: boundary files exist"</automated>
  </verify>
  <done>Both data/CLAUDE.md and data/.claude/settings.json exist and are non-empty, confirming the project boundary is intact</done>
</task>

</tasks>

<verification>
1. `grep "settingSources" src/lib/invoke-agent.ts` shows `['project']`
2. `data/CLAUDE.md` and `data/.claude/settings.json` both exist
3. No other files modified
</verification>

<success_criteria>
- invoke-agent.ts uses `settingSources: ['project']` instead of `settingSources: []`
- Project boundary files confirmed present
- Next agent invocation will load desk CLAUDE.md and symlinked skills
</success_criteria>

<output>
After completion, create `.planning/quick/260328-c3k-fix-skills-tools-not-being-used-by-agent/260328-c3k-SUMMARY.md`
</output>
