---
phase: quick-260329-0ea
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/NeuralHero.tsx
  - public/cortex.css
autonomous: true
requirements: [QUICK-01]
must_haves:
  truths:
    - "Canvas shows ~55 nodes with 6 v3 colors drifting slowly"
    - "Connections appear as blue-tinted lines when nodes are within 120px"
    - "Firing nodes glow red with white core and connected edges turn red"
    - "Firing propagates via setTimeout with 200-800ms random delay"
    - "Hero background matches v3 gradient (0a0a1a -> 1a0a2e -> 0a1a2e)"
    - "Stats use v3 color scheme: accent red, green, blue, amber"
  artifacts:
    - path: "src/app/components/NeuralHero.tsx"
      provides: "V3-style neural canvas animation"
    - path: "public/cortex.css"
      provides: "V3-style hero styling"
  key_links:
    - from: "NeuralHero.tsx canvas"
      to: "cortex.css .dashboard-hero"
      via: "className and inline style"
---

<objective>
Replace the current NeuralHero canvas animation with the simpler, more elegant animation from myelin-v3.

Purpose: The v3 animation is cleaner — single glow gradient, setTimeout-based firing with white core flash, red firing connections. The current version has overly complex layered glows.
Output: Updated NeuralHero.tsx and cortex.css matching v3 visual style exactly.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/components/NeuralHero.tsx
@public/cortex.css (lines 1302-1392, Section 26 hero styles)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port v3 canvas animation to NeuralHero.tsx</name>
  <files>src/app/components/NeuralHero.tsx</files>
  <action>
Rewrite the canvas animation logic in NeuralHero.tsx to match v3 exactly. Keep the React component interface (NeuralHeroProps with title, subtitle, stats with optional color).

**Node generation:**
- Exactly 55 nodes (remove hub clustering and Gaussian distribution — v3 uses simple random placement)
- 6 colors as hex strings: ['#e94560','#6496ff','#00d68f','#ff64c8','#ffb347','#a0f0ff']
- Normalized coordinates (0-1) for x/y
- Slow drift: vx/vy = (Math.random() - 0.5) * 0.0016 (normalized, so ~0.0008 effective)
- Each node has: pulseSpeed (0.02-0.04 random), pulsePhase (random), fireTimer (starts 0)
- Base radius: small dots, ~1.5-2.5px

**Node interface:** { x, y, vx, vy, radius, pulseSpeed, pulsePhase, fireTimer, color (hex string) }

**Animation loop:**
- Update positions: x += vx, y += vy (already normalized). Bounce off edges (0.02/0.98 bounds).
- Pulse: `pulseRadius = radius + Math.sin(pulsePhase) * 2` (range 0.5-4px variation). Increment pulsePhase by pulseSpeed each frame.

**Connections:**
- Check all pairs. If distance < 120px (pixel space), draw line.
- Default: `rgba(100,150,255, (1 - dist/120) * 0.25)`, lineWidth 1.
- If EITHER node is firing (fireTimer > 0): `rgba(233,69,96, (1 - dist/120) * 0.25 * 3)`, lineWidth 1.5.

**Node rendering (use 'lighter' composite for glow):**
- Single radial gradient glow: from `{color}80` (hex alpha 50%) at center to `{color}00` (transparent) at radius * 4.
- If firing (fireTimer > 0): glow color = `#e9456080` center to `#e9456000` edge, and core fill = white (#ffffff).
- If not firing: glow uses node's own color, core fill = node's color (solid).
- Core dot: filled circle at base radius.

**Firing system (replace frame-count-based with setTimeout):**
- On mount, start a recurring setTimeout chain: after random 200-800ms, pick a random node, set its fireTimer = 1.0, then schedule the next fire.
- fireTimer decays at 0.03 per frame (not 0.015).
- No propagation delay (fireDelay) — v3 doesn't have it. Remove the neighbor propagation entirely.
- Clean up the setTimeout on unmount.

**Remove:** boxMuller function, hub cluster generation, NEON_COLORS array (replace with hex array), fireDelay field, neighbor propagation logic, the 3-layer glow system (outer/medium/inner), brightness field.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && npx tsc --noEmit src/app/components/NeuralHero.tsx 2>&1 | head -20</automated>
  </verify>
  <done>NeuralHero.tsx compiles without errors. Animation uses v3 logic: 55 nodes, setTimeout firing, single glow gradient, white core on fire, red connections on fire, slow drift.</done>
</task>

<task type="auto">
  <name>Task 2: Update cortex.css hero styles to match v3</name>
  <files>public/cortex.css</files>
  <action>
Update Section 26 (lines 1302-1392) in cortex.css to match v3 styling:

**`.dashboard-hero`:**
- Change height from 400px to min-height: 180px (remove fixed height, use min-height)
- Keep position: relative, overflow: hidden
- The inline background gradient in JSX will be updated in Task 1 to: `linear-gradient(135deg, #0a0a1a, #1a0a2e, #0a1a2e)`

**`.dashboard-hero::after` (bottom fade):**
- Update gradient end color from #090816 to #0a0a1a to match v3 background

**`.dashboard-hero canvas`:**
- Change opacity from 0.7 to 0.8

**`.dashboard-hero-title`:**
- Keep gradient text effect but it stays as-is (component passes custom title)

**`.dashboard-hero-subtitle`:**
- Change font-size from 16px to 11px
- Change color to #a0a0d0
- Change letter-spacing to 0.15em (from 0.4em)

**`.dashboard-hero-stats`:**
- Keep flex layout

**`.dashboard-hero-stat-value`:**
- Change font-size from 36px to 28px
- Keep font-weight: 900 (v3 uses bold, 900 is fine)

**`.dashboard-hero-stat-label`:**
- Change font-size from 11px to 10px
- Change color to #8080b0 (from rgba(255,255,255,0.35))

**Stat color classes — replace the four stat-* classes with v3 colors:**
- `.stat-accent .dashboard-hero-stat-value { color: #e94560; text-shadow: 0 0 20px rgba(233,69,96,0.4); }`
- `.stat-green .dashboard-hero-stat-value { color: #00d68f; text-shadow: 0 0 20px rgba(0,214,143,0.4); }`
- `.stat-blue .dashboard-hero-stat-value { color: #6496ff; text-shadow: 0 0 20px rgba(100,150,255,0.4); }`
- `.stat-amber .dashboard-hero-stat-value { color: #ffb347; text-shadow: 0 0 20px rgba(255,179,71,0.4); }`
- Remove stat-cyan, stat-mint, stat-magenta classes

Also update the inline background style in Task 1's NeuralHero.tsx to use `linear-gradient(135deg, #0a0a1a, #1a0a2e, #0a1a2e)`.

**Also update `.dashboard-hero-overlay` padding** from `40px 20px` to `20px` to fit the smaller min-height.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && grep -c "stat-cyan\|stat-mint\|stat-magenta" public/cortex.css | grep "^0$" && echo "Old stat classes removed" || echo "FAIL: old stat classes still present"</automated>
  </verify>
  <done>Hero CSS matches v3: min-height 180px, 0.8 canvas opacity, 11px subtitle in #a0a0d0, 28px stat values, v3 stat colors (accent/green/blue/amber), 10px labels in #8080b0.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for NeuralHero.tsx
- No old stat color classes (cyan/mint/magenta) in cortex.css
- Hero section visually matches v3: compact height, slow-drifting nodes, blue connections, red firing with white cores
</verification>

<success_criteria>
- NeuralHero canvas animation matches v3 behavior: 55 nodes, 6 colors, slow drift, setTimeout firing, single glow, white core on fire, red connections on fire
- CSS matches v3 styling: compact hero, smaller text, v3 color palette for stats
- Component interface unchanged (title, subtitle, stats props still work)
</success_criteria>

<output>
After completion, create `.planning/quick/260329-0ea-copy-myelin-v3-neural-brain-canvas-anima/260329-0ea-SUMMARY.md`
</output>
