---
type: quick
description: "Give planning agents full tool access by removing tools:[] restriction"
files_modified:
  - src/app/api/tamir/route/route.ts
  - src/app/api/tasks/[taskId]/message/route.ts
  - src/lib/workspace.ts
---

<objective>
Remove the `tools: []` restriction from planning invocations and update the planning desk CLAUDE.md to reflect that agents now have full built-in tool access (Read, Write, Bash, Glob, Grep, etc.) within their sandboxed planning desk directory. Phase 06 filesystem sandboxing makes this safe — the canUseTool callback restricts all file operations to the agent's desk directory.

Purpose: Agents will produce better plans because they can research the codebase, read files, and search for patterns during planning sessions.
Output: Planning agents invoke with default claude_code preset tools; CLAUDE.md accurately describes available tools.
</objective>

<context>
@src/app/api/tamir/route/route.ts
@src/app/api/tasks/[taskId]/message/route.ts
@src/lib/workspace.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tools:[] from planning invocations</name>
  <files>src/app/api/tamir/route/route.ts, src/app/api/tasks/[taskId]/message/route.ts</files>
  <action>
In `src/app/api/tamir/route/route.ts` line 48: Remove the entire line `tools: [],  // Planning-only: no built-in tools needed, just LLM inference`. Do NOT replace it with anything — omitting the `tools` property causes the SDK to use the default claude_code preset which includes all built-in tools.

In `src/app/api/tasks/[taskId]/message/route.ts` line 97: Remove the entire line `tools: [],  // Planning-only: no built-in tools needed, just LLM inference`. Same reasoning — let the SDK default apply.
  </action>
  <verify>grep -n "tools: \[\]" src/app/api/tamir/route/route.ts src/app/api/tasks/\[taskId\]/message/route.ts — should return no matches</verify>
  <done>Neither planning route passes tools:[] to orchestrator.invoke</done>
</task>

<task type="auto">
  <name>Task 2: Update planning desk CLAUDE.md template</name>
  <files>src/lib/workspace.ts</files>
  <action>
In `ensurePlanningDesks()` (line 84+), replace the CLAUDE.md template string (lines 109-150) with an updated version that:

1. Keeps the "Planning Desk: ${dept}" heading and "PLANNING MODE" section
2. Keeps the MCP tools list (read_memory, write_memory, read_knowledge, etc.)
3. REMOVES the "What You Cannot Do in Planning Mode" section entirely (lines 138-141 about built-in tools being DISABLED)
4. ADDS a new section:

```
## Built-in Tools

You have full access to Claude's built-in tools (Read, Write, Bash, Edit, Glob, Grep, WebSearch, etc.).
Your filesystem access is sandboxed to this planning desk directory. Use these tools to:
- Read source files to understand the codebase before planning
- Search for patterns with Glob/Grep to inform your recommendations
- Check existing implementations to avoid redundant work

Do NOT use built-in tools to modify production code during planning. Planning mode is for research and plan creation only.
```

5. Keeps the "Planning Guidelines" section, updating the first bullet to: "Use built-in tools (Read, Glob, Grep) and `read_memory` to research the codebase before planning"
  </action>
  <verify>grep -c "DISABLED" src/lib/workspace.ts — should return 0. grep -c "sandboxed" src/lib/workspace.ts — should return at least 1.</verify>
  <done>Planning desk CLAUDE.md template accurately describes that built-in tools are available and sandboxed, with no mention of tools being disabled</done>
</task>

</tasks>

<verification>
1. `npx next build 2>&1 | tail -5` — TypeScript compilation succeeds (no type errors from removed properties)
2. `grep -rn "tools: \[\]" src/app/api/` — returns no matches
3. `grep "DISABLED" src/lib/workspace.ts` — returns no matches
</verification>

<success_criteria>
- Planning invocations no longer pass tools:[] (agents get default built-in tools)
- Planning desk CLAUDE.md template accurately reflects sandboxed built-in tool access
- Build passes without errors
</success_criteria>
