---
phase: quick
plan: 260328-188
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/workspace.ts
autonomous: true
must_haves:
  truths:
    - "Planning desk CLAUDE.md lists all available MCP tools with descriptions"
    - "Planning desk CLAUDE.md clarifies what tools are NOT available in planning mode"
    - "Stale planning desk CLAUDE.md files are regenerated on server startup"
  artifacts:
    - path: "src/lib/workspace.ts"
      provides: "Updated ensurePlanningDesks() and planning CLAUDE.md content"
  key_links:
    - from: "src/lib/workspace.ts:ensurePlanningDesks()"
      to: "data/departments/*/planning-desk/CLAUDE.md"
      via: "writeFileSync"
      pattern: "writeFileSync.*planningClaudeMd"
---

<objective>
Fix planning desk CLAUDE.md files so agents know about available MCP tools during planning conversations.

Purpose: Agents in planning mode have access to MCP tools (read_memory, search_knowledge, get_dept_status, etc.) but the planning desk CLAUDE.md doesn't mention them. Agents don't know they can research during planning, making plans less informed.

Output: Updated `ensurePlanningDesks()` that always writes a comprehensive CLAUDE.md listing MCP tools, and deletes the `existsSync` guard so stale files get overwritten.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/workspace.ts
@src/lib/mcp/server.ts
@src/app/api/tasks/[taskId]/message/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ensurePlanningDesks() to write comprehensive CLAUDE.md with MCP tool list</name>
  <files>src/lib/workspace.ts</files>
  <action>
In `ensurePlanningDesks()` (line 84-122 of src/lib/workspace.ts), make two changes:

1. **Remove the `existsSync` guard** on line 109: Change `if (!existsSync(planningClaudeMd))` to always write the file. This ensures stale CLAUDE.md files from earlier versions get overwritten on every server startup. The file is cheap to regenerate.

2. **Replace the sparse CLAUDE.md content** (lines 110-119) with a comprehensive planning-mode guide. The new content should be:

```
# Planning Desk: ${dept}

## Mode

You are in PLANNING MODE. Help the CEO plan a task. Do NOT execute anything.
Ask clarifying questions, gather information using your tools, then produce a detailed plan when ready.

## Available Tools

You have MCP tools available during planning. USE THEM to research and inform your plans:

### Research & Memory
- \`read_memory\` — Read your persistent memory (past projects, conventions, notes)
- \`write_memory\` — Save important context to your memory for future reference
- \`read_knowledge\` — Read department knowledge base files
- \`write_knowledge\` — Add to department knowledge base
- \`search_knowledge\` — Full-text search across department knowledge

### Organization
- \`get_dept_status\` — Check active tasks and employee counts across departments
- \`read_inbox\` — Check your notification inbox

### Other (available but typically used during execution)
- \`promote_to_deliverable\` — Move files to deliverables (execution phase)
- \`file_to_vault\` — Save files to company vault (execution phase)
- \`submit_for_review\` — Submit work for review (execution phase)
- \`propose_skill\` — Propose a reusable skill (execution phase)
- \`hire_employee\` — Request a temp hire (execution phase)

## What You Cannot Do in Planning Mode

Built-in tools (Read, Write, Bash, Edit, Glob, Grep) are DISABLED during planning.
You cannot read files, run commands, or modify the codebase. Use MCP tools above for research.

## Planning Guidelines

- Use \`read_memory\` to recall past work and conventions before planning
- Use \`search_knowledge\` to find relevant department knowledge
- Use \`get_dept_status\` to understand current workload before scoping
- Ask the CEO clarifying questions when requirements are ambiguous
- Produce a detailed plan with clear steps when you have enough information
```

Note: Keep the existing `createTaskWorkspace()` function unchanged — it already has a good CLAUDE.md for execution workspaces.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ensurePlanningDesks() always overwrites CLAUDE.md (no existsSync guard)
    - Planning desk CLAUDE.md lists all 12 MCP tools grouped by planning relevance
    - CLAUDE.md explains that built-in tools (Read/Write/Bash) are disabled in planning mode
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete stale planning desk CLAUDE.md files so they regenerate on next startup</name>
  <files>data/departments/tech/planning-desk/CLAUDE.md, data/departments/marketing/planning-desk/CLAUDE.md, data/departments/operations/planning-desk/CLAUDE.md, data/departments/global/planning-desk/CLAUDE.md</files>
  <action>
Delete all 4 existing planning desk CLAUDE.md files:

```bash
rm data/departments/tech/planning-desk/CLAUDE.md
rm data/departments/marketing/planning-desk/CLAUDE.md
rm data/departments/operations/planning-desk/CLAUDE.md
rm data/departments/global/planning-desk/CLAUDE.md
```

Then start the dev server briefly to trigger instrumentation.ts -> ensurePlanningDesks() which will regenerate them with the new comprehensive content. Verify one of the regenerated files contains "read_memory" and "Available Tools".

If the server can't be started (port conflict, etc.), manually call ensurePlanningDesks() via a one-liner:
```bash
cd /home/omersh/myelin-gsd && node -e "require('./src/lib/workspace').ensurePlanningDesks()"
```
Or simply verify the code is correct and the files will regenerate on next startup — the stale files are deleted either way.
  </action>
  <verify>
    <automated>grep -l "read_memory" /home/omersh/myelin-gsd/data/departments/*/planning-desk/CLAUDE.md 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    - All 4 planning desk CLAUDE.md files contain the new comprehensive content with MCP tool listings
    - OR stale files are deleted and will regenerate on next server startup
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Planning desk CLAUDE.md files contain "Available Tools" and "read_memory"
3. Planning desk CLAUDE.md files explain that built-in tools are disabled
4. No regression in createTaskWorkspace() — execution workspace CLAUDE.md unchanged
</verification>

<success_criteria>
- Agents in planning mode see a CLAUDE.md that lists all available MCP tools
- Agents understand which tools are for planning (research) vs execution
- Agents know built-in tools (Read/Write/Bash) are disabled in planning
- Stale CLAUDE.md files are always overwritten on server startup
</success_criteria>

<output>
After completion, create `.planning/quick/260328-188-fix-mcp-tools-and-skills-not-showing-dur/260328-188-SUMMARY.md`
</output>
