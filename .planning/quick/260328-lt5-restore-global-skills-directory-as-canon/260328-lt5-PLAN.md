---
phase: quick
plan: 260328-lt5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/workspace.ts
  - src/lib/seed-gallery.ts
  - data/departments/global/skills/memory-management/SKILL.md
  - data/departments/global/skills/system-reset/SKILL.md
  - data/departments/global/skills/skill-extractor/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "data/departments/global/skills/ contains memory-management, system-reset, skill-extractor"
    - "data/departments/cos/skills/ contains ONLY installer"
    - "All agent desks and task workspaces get global/skills symlinked"
    - "Only Tamir's desk gets cos/skills symlinked"
    - "Gallery seeding reads global skills from global/skills/ with department 'global'"
  artifacts:
    - path: "data/departments/global/skills/"
      provides: "Canonical global skills directory"
    - path: "src/lib/workspace.ts"
      provides: "Updated symlink logic for global vs cos separation"
    - path: "src/lib/seed-gallery.ts"
      provides: "Gallery seeding from global/skills/"
  key_links:
    - from: "src/lib/workspace.ts"
      to: "data/departments/global/skills/"
      via: "symlinkSync in createTaskWorkspace and ensurePlanningDesks"
      pattern: "global.*skills"
---

<objective>
Restore data/departments/global/ as a dedicated global skills directory (NOT a department), separate from cos/ which holds Tamir-specific skills only.

Purpose: Global skills (memory-management, system-reset, skill-extractor) should be symlinked into ALL agent desks and workspaces. Tamir-specific skills (installer) should only be in Tamir's desk. Currently everything lives in cos/skills/ with no distinction.

Output: Separated skill directories, updated symlink logic, updated gallery seeding.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/workspace.ts
@src/lib/seed-gallery.ts
@data/departments/cos/skills/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move global skills to data/departments/global/skills/ and clean cos/skills/</name>
  <files>
    data/departments/global/skills/memory-management/SKILL.md
    data/departments/global/skills/system-reset/SKILL.md
    data/departments/global/skills/skill-extractor/SKILL.md
    data/departments/cos/skills/
  </files>
  <action>
    1. Create directory `data/departments/global/skills/`
    2. Move (cp -r then rm -rf) the following from `data/departments/cos/skills/` to `data/departments/global/skills/`:
       - memory-management/
       - system-reset/
       - skill-extractor/
    3. Leave `data/departments/cos/skills/installer/` in place — this is Tamir-specific
    4. Do NOT create any other subdirectories in `data/departments/global/` (no planning-desk, manager-desk, tools, etc.) — only skills/
    5. Verify final state:
       - `data/departments/global/skills/` has memory-management, system-reset, skill-extractor
       - `data/departments/cos/skills/` has ONLY installer
  </action>
  <verify>
    <automated>ls data/departments/global/skills/ | sort && echo "---" && ls data/departments/cos/skills/ | sort && echo "---" && ls data/departments/global/ | sort</automated>
  </verify>
  <done>global/skills/ has 3 global skills, cos/skills/ has only installer, global/ has only skills/ subdirectory</done>
</task>

<task type="auto">
  <name>Task 2: Update workspace.ts symlink logic for global vs cos separation</name>
  <files>src/lib/workspace.ts</files>
  <action>
    Update three functions in `src/lib/workspace.ts`:

    **A. `createTaskWorkspace()`** (lines ~50-63):
    - Change the `cosSkillsSource` variable to point at `data/departments/global/skills/` and rename it to `globalSkillsSource`
    - Change `cosSkillsTarget` to `globalSkillsTarget` with target name `'global'` (not `'cos'`)
    - This ensures ALL task workspaces get global skills symlinked (memory-management, system-reset, skill-extractor)
    - Do NOT symlink cos/skills into task workspaces — cos/skills (installer) is Tamir-only

    **B. `ensurePlanningDesks()`** — the `dept === 'cos'` block (lines ~182-248):
    - Currently symlinks cos/skills into Tamir's .claude/skills/cos
    - ADD a second symlink: global/skills -> .claude/skills/global (so Tamir gets both global AND cos skills)
    - Keep the cos symlink for Tamir's desk (installer skill)

    **C. `ensurePlanningDesks()`** — the non-cos departments block (lines ~252-329):
    - Currently symlinks cos/skills into planning desks as `cosSkillsTarget`
    - Change this to symlink `global/skills` instead (rename source to globalSkillsSource, target to 'global')
    - Do NOT symlink cos/skills into non-Tamir planning desks
    - Keep the department's own skills symlink (deptSkillsSource -> deptSkillsTarget)

    **D. `ensureManagerDesks()`** (lines ~131-176):
    - Currently does NOT symlink any skills into manager desks
    - No changes needed here — manager desks are for approval reviews, not skill execution

    Important: Do NOT add 'global' to the DEPARTMENTS array — global is not a real department.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && npx tsc --noEmit src/lib/workspace.ts 2>&1 | head -20</automated>
  </verify>
  <done>workspace.ts compiles cleanly with global skills symlinked to all desks/workspaces, cos skills symlinked only to Tamir's desk</done>
</task>

<task type="auto">
  <name>Task 3: Update seed-gallery.ts to read skills from global/skills/</name>
  <files>src/lib/seed-gallery.ts</files>
  <action>
    Update `seedSkills()` in `src/lib/seed-gallery.ts`:

    1. Currently reads from `skills/` (project root) which contains symlinks to `.agents/skills/` — these are externally installed skills, NOT the global skills we need to seed.

    2. Add a second seeding source: `data/departments/global/skills/`
       - Read each subdirectory's SKILL.md, parse frontmatter
       - Upsert with `department: 'global'` (not 'cos')
       - This seeds memory-management, system-reset, skill-extractor into the gallery

    3. Also add seeding for `data/departments/cos/skills/`:
       - Upsert with `department: 'cos'`
       - This seeds installer into the gallery

    4. Keep the existing `skills/` (project root) seeding for externally installed skills — these stay as `department: 'cos'` or whichever is appropriate (keep existing behavior).

    5. Consider also seeding department-specific skills from `data/departments/{tech,marketing,operations}/skills/` with their respective department values. This future-proofs the gallery for department skills.

    The key change: global skills should show as `department: 'global'` in the gallery, not 'cos'.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && npx tsc --noEmit src/lib/seed-gallery.ts 2>&1 | head -20</automated>
  </verify>
  <done>seed-gallery.ts compiles, seeds global skills with department='global', cos skills with department='cos', and external skills from project root</done>
</task>

</tasks>

<verification>
1. `ls data/departments/global/skills/` shows memory-management, system-reset, skill-extractor
2. `ls data/departments/cos/skills/` shows only installer
3. `ls data/departments/global/` shows only skills (no planning-desk, manager-desk, tools)
4. `npx tsc --noEmit` passes for both modified source files
5. Grep workspace.ts for 'global/skills' confirms new symlink paths
6. Grep seed-gallery.ts for 'global' confirms new seeding source
</verification>

<success_criteria>
- Global skills directory exists with 3 skills, separate from cos
- All agent desks and workspaces get global skills via symlink
- Only Tamir's desk gets cos (installer) skill via symlink
- Gallery seeds global skills with department='global'
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260328-lt5-restore-global-skills-directory-as-canon/260328-lt5-SUMMARY.md`
</output>
