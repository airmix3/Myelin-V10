---
phase: quick-260328-x69
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/cortex.css
  - src/app/components/NeuralHero.tsx
  - src/app/DashboardClient.tsx
autonomous: false
requirements: [POLISH-01]
must_haves:
  truths:
    - "Hero section feels premium with refined typography, accent-colored stat values, and breathing room"
    - "Hero-to-org-graph transition is seamless with gradient fade blending"
    - "Activity overlay has a terminal/hacker aesthetic with monospace font and scanline feel"
    - "Stat counters use neon accent colors matching the neural palette"
    - "Activity entries are well-spaced with readable, distinct badges"
  artifacts:
    - path: "public/cortex.css"
      provides: "Polished dashboard styles (Section 26)"
      contains: "dashboard-hero"
    - path: "src/app/components/NeuralHero.tsx"
      provides: "Updated hero with gradient bottom fade"
    - path: "src/app/DashboardClient.tsx"
      provides: "Accent-colored stat values passed to hero"
  key_links:
    - from: "src/app/DashboardClient.tsx"
      to: "src/app/components/NeuralHero.tsx"
      via: "stats prop with color field"
      pattern: "stats.*color"
    - from: "public/cortex.css"
      to: "src/app/DashboardClient.tsx"
      via: "CSS class names"
      pattern: "dashboard-hero|activity-overlay"
---

<objective>
Polish the dashboard UI for a premium, founder-grade look — refine hero typography and spacing, add seamless hero-to-org-graph blending, style stat counters with neon accent colors, and give the activity overlay a terminal aesthetic.

Purpose: Make the Cortex dashboard feel like a high-end command center, not a generic dashboard.
Output: Updated cortex.css (Section 26), NeuralHero.tsx, DashboardClient.tsx
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@public/cortex.css (lines 1302-1493 — Section 26 dashboard styles)
@src/app/components/NeuralHero.tsx
@src/app/DashboardClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish hero typography, stat colors, and hero-to-org blending</name>
  <files>public/cortex.css, src/app/components/NeuralHero.tsx, src/app/DashboardClient.tsx</files>
  <action>
CSS changes in `public/cortex.css` Section 26 (lines 1302-1493) — modify existing rules, do NOT duplicate them:

**Hero container (.dashboard-hero):**
- Increase height from 350px to 400px for more breathing room
- Add a pseudo-element `::after` that creates a 80px gradient fade from hero bottom into the org graph below: `background: linear-gradient(to bottom, transparent, #090816)` positioned at the bottom, ensuring seamless blending with no hard edge

**Title (.dashboard-hero-title):**
- Increase letter-spacing from 0.15em to 0.2em
- Add a subtle background-clip text gradient: use `background: linear-gradient(135deg, #fff 0%, #8FE7FF 50%, #6496FF 100%)` with `-webkit-background-clip: text` and `-webkit-text-fill-color: transparent`
- Increase text-shadow spread for more glow: `0 0 60px rgba(143,231,255,0.4), 0 0 120px rgba(100,150,255,0.2)`

**Subtitle (.dashboard-hero-subtitle):**
- Increase font-size from 14px to 16px
- Change color from var(--text-dim) to rgba(143,231,255,0.5) for a subtle cyan tint
- Increase letter-spacing to 0.4em
- Add margin-bottom: 40px (from 32px)

**Stats container (.dashboard-hero-stats):**
- Increase gap from 40px to 56px

**Stat values (.dashboard-hero-stat-value):**
- Increase font-size from 28px to 36px
- Remove the generic text-shadow
- Add a class-based accent system: add `.stat-cyan .dashboard-hero-stat-value { color: #8FE7FF; text-shadow: 0 0 20px rgba(143,231,255,0.4); }`, `.stat-blue ...` with #6496FF, `.stat-mint ...` with #2EE6A6, `.stat-magenta ...` with #FF64C8

**Stat labels (.dashboard-hero-stat-label):**
- Increase font-size from 10px to 11px
- Change color from var(--text-dim) to rgba(255,255,255,0.35) for slightly more visibility
- Increase letter-spacing to 0.15em
- Add margin-top: 6px (from 4px)

**TSX changes in DashboardClient.tsx:**
- Add a `color` field to the heroStats array items: Agents=cyan, Tasks=blue, Approvals=magenta, Deliverables=mint
- Pass these colors to NeuralHero stats prop

**TSX changes in NeuralHero.tsx:**
- Update the stats interface to include optional `color?: string`
- Apply the color as a className on the stat wrapper div: `className={\`dashboard-hero-stat stat-${s.color || 'cyan'}\`}`
- Do NOT touch the canvas animation code at all
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Hero has gradient text title, larger stats with accent colors per stat, subtitle has cyan tint, hero fades seamlessly into org graph section below</done>
</task>

<task type="auto">
  <name>Task 2: Terminal-aesthetic activity overlay and entry polish</name>
  <files>public/cortex.css</files>
  <action>
CSS changes in `public/cortex.css` Section 26 — modify existing activity overlay rules:

**Activity overlay (.activity-overlay):**
- Change background to `rgba(9,8,22,0.92)` (slightly more opaque for readability)
- Add `border-top: 2px solid rgba(46,230,166,0.3)` for a mint accent line at top
- Change border from `rgba(100,150,255,0.15)` to `rgba(46,230,166,0.1)` for green terminal feel
- Add `box-shadow: 0 4px 30px rgba(0,0,0,0.5), inset 0 1px 0 rgba(46,230,166,0.05)`
- Add `border-radius: 8px` (from 6px)

**Activity header (.activity-overlay-header):**
- Change color from #6496FF to #2EE6A6 (mint green — terminal prompt color)
- Add `font-family: 'Courier New', 'Consolas', monospace` for terminal feel
- Change font-size from 10px to 11px
- Add a `> _` blinking cursor after text via `::after` pseudo-element: `content: '_'; animation: blink 1s step-end infinite; margin-left: 4px; color: #2EE6A6;`

**Activity header blink-dot:**
- Change background from #00d68f to #2EE6A6 for consistency

**Activity body (.activity-overlay-body):**
- Add `font-family: 'Courier New', 'Consolas', monospace`
- Increase font-size from 11px to 12px
- Change padding from `8px 0` to `6px 0`

**Activity entries (.activity-entry):**
- Increase padding from `3px 14px` to `5px 14px` for better breathing room
- Add `border-left: 2px solid transparent` and on hover change to `border-left-color: rgba(46,230,166,0.3)` for a terminal selection feel
- Change hover background to `rgba(46,230,166,0.04)` (mint tint instead of blue)
- Increase gap from 6px to 8px

**Activity timestamps (.activity-ts):**
- Change color from #555 to rgba(143,231,255,0.4) (dim cyan — like terminal line numbers)
- Add `font-family: 'Courier New', 'Consolas', monospace`

**Activity badges (.activity-badge):**
- Increase font-size from 9px to 10px
- Increase padding from `1px 5px` to `2px 6px`
- Add `border-radius: 2px` (from 3px — more angular/terminal)
- Add `font-family: 'Courier New', 'Consolas', monospace`
- Add `letter-spacing: 0.05em`

**Activity description (.activity-desc):**
- Change color from var(--text-dim) to rgba(255,255,255,0.55) for better readability
- Add `font-family: 'Courier New', 'Consolas', monospace`

**Activity agent (.activity-agent):**
- Add `font-family: 'Courier New', 'Consolas', monospace`
- Increase font-size from 10px to 11px
- Add `font-weight: 600`

**Scrollbar (.activity-overlay-body::-webkit-scrollbar-thumb):**
- Change background from `rgba(100,150,255,0.2)` to `rgba(46,230,166,0.2)` to match mint theme
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Activity overlay has monospace terminal aesthetic with mint green accents, blinking cursor in header, better-spaced entries with hover highlight, and more readable text</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dashboard visual polish: premium hero with gradient title, accent-colored stats, seamless hero-to-org blending, and terminal-style activity overlay</what-built>
  <how-to-verify>
    1. Start dev server: `cd /home/omersh/myelin-gsd && node_modules/.bin/next dev -p 3011`
    2. Open http://localhost:3011 in browser
    3. Verify hero section: title should have gradient cyan-blue text, subtitle should have cyan tint, stat values should be colored (cyan for Agents, blue for Tasks, magenta for Approvals, mint for Deliverables)
    4. Verify hero-to-org-graph transition: should fade smoothly with no hard edge between hero and graph
    5. Verify activity overlay (top-right of org graph): should have green terminal aesthetic with monospace font, blinking cursor in header, mint-colored accents, and well-spaced entries
    6. Scroll activity entries if any exist — verify readability and hover effects
  </how-to-verify>
  <resume-signal>Type "approved" or describe what needs adjusting</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All existing dashboard functionality preserved (neural hero animation, org graph, activity overlay, SSE updates)
- No Tailwind or CSS framework usage — custom CSS only
</verification>

<success_criteria>
- Hero feels premium: gradient title, accent-colored stats, refined spacing
- Hero-to-org-graph transition is seamless with no hard edge
- Activity overlay has distinctive terminal/hacker aesthetic
- All typography is refined with proper hierarchy
- All existing functionality unchanged
</success_criteria>

<output>
After completion, create `.planning/quick/260328-x69-review-and-polish-dashboard-ui-for-found/260328-x69-SUMMARY.md`
</output>
