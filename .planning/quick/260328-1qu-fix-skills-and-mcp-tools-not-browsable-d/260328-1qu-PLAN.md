---
phase: quick
plan: 260328-1qu
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seed-gallery.ts
  - src/instrumentation.ts
  - src/components/CanvasPanel.tsx
autonomous: true
requirements: [QUICK-1qu]
must_haves:
  truths:
    - "CEO can see MCP tools listed in the gallery when viewing a plan"
    - "CEO can see skills listed in the gallery when viewing a plan"
    - "Gallery section is visible without excessive scrolling"
  artifacts:
    - path: "src/lib/seed-gallery.ts"
      provides: "Idempotent seeding of mcp_servers and skills tables"
    - path: "src/instrumentation.ts"
      provides: "Calls seedGallery on server startup"
  key_links:
    - from: "src/lib/seed-gallery.ts"
      to: "mcp_servers table"
      via: "prisma.mcpServer.upsert"
      pattern: "prisma\\.mcpServer"
    - from: "src/lib/seed-gallery.ts"
      to: "skills table"
      via: "prisma.skill.upsert"
      pattern: "prisma\\.skill"
---

<objective>
Fix the tool/skill gallery showing "No matching tools" and "No matching skills" during task planning.

**Root cause:** The `mcp_servers` and `skills` database tables are empty -- no records were ever seeded. The UI chain works correctly (CanvasPanel -> ConfigPanel -> GalleryPanel -> search API -> DB), but the DB returns 0 results. Additionally, the config+gallery section in CanvasPanel is cramped at 220px maxHeight making it hard to browse.

Purpose: CEO needs to browse and select MCP tools and skills during planning to attach them to task execution.
Output: Seeded gallery data visible in the planning UI.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/seed-agents.ts (pattern for idempotent seeding)
@src/instrumentation.ts (where to hook seed call)
@src/components/CanvasPanel.tsx (UI layout fix)
@src/components/ConfigPanel.tsx (renders GalleryPanel, already correct)
@src/components/GalleryPanel.tsx (gallery component, already correct)
@src/app/api/search/tools/route.ts (queries prisma.mcpServer)
@src/app/api/search/skills/route.ts (queries prisma.skill)
@src/lib/mcp/server.ts (lists all 8 MCP tool modules)
@skills/agent-browser/SKILL.md (on-disk skill to seed)
@prisma/schema.prisma (McpServer and Skill models)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gallery seed script and hook into startup</name>
  <files>src/lib/seed-gallery.ts, src/instrumentation.ts</files>
  <action>
Create `src/lib/seed-gallery.ts` with an idempotent `seedGallery()` function (following the pattern in `seed-agents.ts`). It does two things:

**1. Seed mcp_servers table** with the Myelin internal MCP server entry:
- Use `prisma.mcpServer.upsert` (upsert on name uniqueness)
- Single record: name="myelin", description="Myelin internal tools (memory, knowledge, vault, deliverable, review, skills, inbox, hire)", status="active", toolCount=14 (approximate count from server.ts which aggregates 8 tool modules)
- id: use `generateId('mcp')`

**2. Seed skills table** with on-disk skills:
- Read `skills/` directory at project root
- For each subdirectory, read `SKILL.md`, parse YAML frontmatter with `gray-matter` to extract `name` and `description`
- Use `prisma.skill.upsert` (upsert where name+department match)
- Set department="global", status="active", filePath to the SKILL.md path, proposedBy="system"
- id: use `generateId('skill')`
- Currently only `agent-browser` exists, but this is future-proof for new skills

**3. Hook into instrumentation.ts:**
- Add a new step between step 4 (seed agents) and step 5 (init orchestrator)
- Dynamic import `seedGallery` from `./lib/seed-gallery`
- Call `await seedGallery()` in try/catch with logger, same pattern as seedAgents
- Log: 'Gallery data seeded (MCP servers + skills)'
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && node -e "
const Database = require('better-sqlite3');
const db = new Database('data/myelin.db');
// Check that seed-gallery.ts compiles
require('child_process').execSync('npx tsc --noEmit src/lib/seed-gallery.ts 2>&1 || true');
console.log('File exists:', require('fs').existsSync('src/lib/seed-gallery.ts'));
"</automated>
  </verify>
  <done>seed-gallery.ts exists with seedGallery() function; instrumentation.ts calls it on startup; after next server start, mcp_servers has at least 1 row and skills has at least 1 row</done>
</task>

<task type="auto">
  <name>Task 2: Expand config+gallery section height in CanvasPanel</name>
  <files>src/components/CanvasPanel.tsx</files>
  <action>
In CanvasPanel.tsx, the config+gallery section (lines 157-161) has `maxHeight: '220px'` which is too cramped for the gallery grid to be usable. Change the layout:

1. Increase `maxHeight` from `'220px'` to `'400px'` -- this gives the ConfigPanel (autonomy slider + budget + constraints + gallery) enough room to display gallery cards without being squished.

2. The plan content area (line 102-135) uses `flex: 1` which is correct -- it will shrink to accommodate the larger config section. The overall layout with `overflow: hidden` on the parent and `overflowY: 'auto'` on both sections means both are independently scrollable.

No other changes needed -- ConfigPanel already imports and renders GalleryPanel correctly, the gallery search APIs work, and the approve route already reads config.selectedTools/selectedSkills/toolHints from task metadata.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && grep -c "maxHeight.*400px" src/components/CanvasPanel.tsx</automated>
  </verify>
  <done>Config+gallery section has 400px maxHeight, gallery cards are visible without excessive scrolling</done>
</task>

</tasks>

<verification>
1. Start dev server: `node_modules/.bin/next dev -p 3011`
2. Check DB after startup: `node -e "const db = require('better-sqlite3')('data/myelin.db'); console.log('mcp_servers:', db.prepare('SELECT * FROM mcp_servers').all()); console.log('skills:', db.prepare('SELECT * FROM skills').all());"`
3. Visit http://localhost:3011, create a task, reach plan stage -- gallery should show company tools and skills
</verification>

<success_criteria>
- mcp_servers table has at least 1 row (myelin MCP server) after server start
- skills table has at least 1 row (agent-browser) after server start
- GalleryPanel shows results when "company" source is selected during planning
- Config+gallery section is 400px tall, making gallery browsable
</success_criteria>

<output>
After completion, create `.planning/quick/260328-1qu-fix-skills-and-mcp-tools-not-browsable-d/260328-1qu-SUMMARY.md`
</output>
