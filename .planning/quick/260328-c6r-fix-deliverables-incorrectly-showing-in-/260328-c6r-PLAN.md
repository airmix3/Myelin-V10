---
phase: quick-260328-c6r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/vault/page.tsx
  - src/lib/fts.ts
autonomous: true
must_haves:
  truths:
    - "Vault page shows only vault and knowledge documents, not deliverables"
    - "Vault FTS5 search returns only vault and knowledge documents, not deliverables"
  artifacts:
    - path: "src/app/vault/page.tsx"
      provides: "Filtered Prisma query excluding deliverables"
      contains: "source"
    - path: "src/lib/fts.ts"
      provides: "Source-filtered FTS5 search"
      contains: "source IN"
  key_links:
    - from: "src/app/vault/page.tsx"
      to: "prisma.document.findMany"
      via: "where clause with source filter"
      pattern: "source.*in.*vault.*knowledge"
    - from: "src/lib/fts.ts"
      to: "documents_fts JOIN documents"
      via: "AND d.source IN clause in SQL"
      pattern: "source IN"
---

<objective>
Fix Vault page showing deliverable documents alongside vault/knowledge documents.

Purpose: The documents table contains 3 sources (vault, knowledge, deliverable). Deliverables are indexed there for unified agent search but should NOT appear in the Vault UI tab. Currently all 16 docs show (13 deliverables + 3 vault).
Output: Vault browse and search both filter to vault+knowledge sources only.
</objective>

<execution_context>
@/home/omersh/myelin-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/omersh/myelin-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/vault/page.tsx
@src/lib/fts.ts
@src/app/vault/VaultClient.tsx
@src/app/api/vault/search/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter Vault Prisma query and FTS5 search to exclude deliverables</name>
  <files>src/app/vault/page.tsx, src/lib/fts.ts</files>
  <action>
1. In `src/app/vault/page.tsx`, add a `where` clause to the `prisma.document.findMany()` call:
   ```
   where: { source: { in: ['vault', 'knowledge'] } }
   ```
   This filters out documents with source='deliverable' from the browse view.

2. In `src/lib/fts.ts`, update the `searchDocuments` function's SQL query (around line 74-84). Add a source filter to the WHERE clause after the MATCH:
   ```sql
   WHERE documents_fts MATCH ?
     AND d.source IN ('vault', 'knowledge')
   ```
   This ensures FTS5 search results also exclude deliverables.

Do NOT modify VaultClient.tsx — the `sourceBadgeClass` for 'deliverable' can remain as defensive code since it costs nothing and protects against future edge cases.
  </action>
  <verify>
    <automated>cd /home/omersh/myelin-gsd && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Vault page query includes source filter for vault+knowledge only. FTS5 search query includes source filter. TypeScript compiles without errors. Deliverables no longer appear in Vault browse or search.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Vault page.tsx contains `source: { in: ['vault', 'knowledge'] }` in the Prisma query
- fts.ts searchDocuments contains `d.source IN ('vault', 'knowledge')` in the SQL
</verification>

<success_criteria>
Vault tab shows only vault and knowledge documents (3 docs), not deliverables (13 docs). Search also excludes deliverables.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-c6r-fix-deliverables-incorrectly-showing-in-/260328-c6r-SUMMARY.md`
</output>
